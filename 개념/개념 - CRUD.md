---
출처: Claude 분석
작성일: 2026-08-13
tags: [개념]
---

# 개념 - CRUD

여러 노트에 걸쳐 반복해서 나온 개념을 모아둔 관문 노트다. 각 지점에서 이 노트로 들어오고, 이 노트에서 다른 지점으로 나간다.

## 뜻

**데이터를 다루는 네 가지 기본 연산** — Create(생성)·Read(조회)·Update(수정)·Delete(삭제)다. 게시판이 이 네 연산을 한 번에 담는 대표 예제라, 언어를 바꿔가며 같은 게시판을 반복해서 만들었다.

| 연산 | 하는 일 | 게시판에서 |
| --- | --- | --- |
| Create | 새 데이터 추가 | 글 작성 |
| Read | 조회 | 목록 / 상세 |
| Update | 기존 데이터 수정 | 글 수정 |
| Delete | 삭제 | 글 삭제 |

## 공통 뼈대

언어와 저장소가 달라도 CRUD 코드는 같은 골격을 따른다.

1. **저장소에서 데이터 묶음을 꺼낸다** (배열/리스트로)
2. **대상을 찾는다** — 식별자(no)로 찾고, 없으면 관례적으로 `-1`
3. **연산한다** — 추가(push/add) · 수정 · 삭제(splice/remove)
4. **저장소에 다시 넣는다**

두 가지가 반복해서 등장한다.

- **자동 번호** — 새 글 번호는 `마지막.no + 1`. SQL의 `AUTO_INCREMENT`를 손으로 흉내 낸 것이다
- **번호(no) ≠ 인덱스** — 3번을 지우면 배열 인덱스는 당겨지지만 no는 유지된다. 그래서 위치가 아니라 `findIndex(b => b.no === no)`로 찾아야 한다

## 이 저장소에서 나타난 지점

| 지점 | 저장소 | 특징 |
| --- | --- | --- |
| [[JS day14 게시판 CRUD]] | `localStorage` + 배열 | 4화면(write/list/view/update), 쿼리스트링으로 상세 연결 |
| [[JS day12 제품 사원 관리 CRUD]] | 배열 | 데이터 모델링, 목록 렌더링 |
| [[Java day06 생성자와 콘솔 게시판]] | `Post[100]` 배열 | 빈 자리(`null`) 찾기, 계층 없이 main에 전부 |
| [[Java day09 MVC 종합예제]] | `ArrayList` + DAO | C·R 구현, 같은 이름 메소드를 세 계층에 나란히 |

핵심 연결: 위 넷은 **같은 게시판을 다른 층·다른 구조로 만든 것**이다. 나란히 놓으면 CRUD의 본질과 "계층을 나누는 이유"가 동시에 보인다.

## CRUD ↔ HTTP ↔ SQL

같은 네 연산이 층을 바꿔도 이름만 달라진다.

| CRUD | localStorage | REST API | SQL |
| --- | --- | --- | --- |
| Create | `setItem`(추가) | `POST /boards` | `INSERT` |
| Read | `getItem` | `GET /boards` | `SELECT` |
| Update | `setItem`(수정) | `PUT /boards/3` | `UPDATE` |
| Delete | `splice` | `DELETE /boards/3` | `DELETE` |

이 대응을 알면 프론트에서 만든 CRUD가 백엔드·DB로 그대로 이어진다. → [[SQL day03 DML과 조인]]

## 함께 보면 좋은 것

- CRUD 로직을 한 곳에 모으면 저장소를 갈아끼우기 쉬워진다. JS에서 `common.js`로 뺀 저장 함수 묶음이 Java에서는 DAO에 해당한다 — 이 계층 분리를 끝까지 밀면 [[Repository Pattern]]이 된다
- 값 검증(빈 제목 막기)은 여러 겹으로 둔다: 화면(형식) → 컨트롤러(업무 규칙) → DTO/DB 제약(최후 방어선). → [[SQL day02 테이블과 제약조건]]
