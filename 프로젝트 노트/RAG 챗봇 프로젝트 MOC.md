---
출처: Claude 대화
작성일: 2026-09-10
tags: [허브]
---

# RAG 챗봇 프로젝트 MOC

데이터시트 PDF를 올리면 질문에 **출처 페이지와 함께** 답하는 챗봇을 만드는 팀 프로젝트의 허브다. 이미 쓰여 있는 코드를 읽으며 "왜 이렇게 되어 있는가"를 이해한 것을 쌓는다.

## 프로젝트 개요

| 항목 | 내용 |
| --- | --- |
| 목표 | 하드웨어 데이터시트 PDF를 인덱싱해두고, 질문하면 근거 페이지를 붙여 답하는 챗봇 |
| 위치 | `Desktop/진로탐색` |
| 구성 | FastAPI 웹 + PDF 인제스천 파이프라인 + Chroma 벡터 DB + 답변 생성 모듈 |
| 언어 | Python |
| 담당 경계 | 업로드부터 검색까지가 내 몫. 검색 결과를 넘겨받아 답변을 만드는 모듈은 팀원 몫 |

## 이 프로젝트의 작성 원칙 (다른 프로젝트와 다른 점)

**물어본 것만 적는다.** 노트 하나가 질문 하나에 대응한다.

수집기·WMS는 **설계 기록**이 목적이라 "무엇을 왜 선택했나"를 쌓는다. 이 프로젝트는 코드가 이미 다 쓰여 있고 그것을 **읽어서 이해하는 것**이 목적이라, 방향이 반대다. 그래서 다음을 지킨다.

- 채팅에서 **실제로 물어본 질문만** 노트가 된다. 물어보지 않은 내용을 곁들여 채우지 않는다
- 노트 제목은 그 질문의 답이 무엇인지로 짓는다
- 번호는 **물어본 순서**다. 이해가 정리되면서 읽는 순서를 바꾸고 싶으면 파일명이 아니라 아래 표의 줄 순서를 옮긴다
- 설계 변경 기록·로드맵 장부는 두지 않는다. 이 프로젝트에서 설계를 정하는 일이 없기 때문이다

## 진행 상태

| 단계 | 상태 |
| --- | --- |
| 업로드 · 저장 · 검증 | 코드 완성 |
| 파싱 · 전처리 · 청킹 | 코드 완성, 청크 덤프로 눈 검증까지 진행 |
| 임베딩 · 벡터 DB 저장 | 코드 완성, 아직 실행 전 |
| 검색 · 답변 연결 | 코드 완성, 아직 실행 전 |
| 프론트엔드 | 없음 |

## 주제 목록

| 주제 | 물어본 것 |
| --- | --- |
| [[RAG 챗봇 01 - 인제스천 파이프라인의 순서]] | PDF 받고 청킹·임베딩·벡터 DB 저장 순서가 맞는가 |
| [[RAG 챗봇 02 - 패키지 초기화 파일이 비어 있는 이유]] | `__init__` 파일은 왜 빈 파일인가 |
| [[RAG 챗봇 03 - 토크나이저를 임베딩 모델에 맞추는 이유]] | 여러 토크나이저 중 이것을 쓰는 이유가 있는가 |
| [[RAG 챗봇 04 - 청킹에 AI가 들어가지 않는 이유]] | 청킹에도 AI가 들어가는가 |
| [[RAG 챗봇 05 - 임베딩과 벡터 검색이 하는 일]] | 검색과 임베딩이 쉽게 말해 무엇을 하는가 |
| [[RAG 챗봇 06 - 구현 범위와 담당 경계]] | 지금 코드는 어디까지 구현되어 있는가 / 내 몫은 어디까지인가 |

## 장부

| 노트 | 용도 |
| --- | --- |
| [[RAG 챗봇 - 규칙제안 기록]] | 이 프로젝트에서 Vault 전체 규칙에 반영을 요청한 제안 목록 |

## 진행 기록

| 날짜 | 내용 |
| --- | --- |
| 2026-09-10 | 코드 전체 읽기 시작. 파이프라인 순서·초기화 파일·토크나이저·청킹의 성격·임베딩 개념·구현 범위를 01~06번으로 정리 |

## 관련 노트

[[프로젝트 노트 MOC]]
