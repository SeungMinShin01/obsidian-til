---
출처: verify-vault 정기 점검
작성일: 2026-09-15
tags: [규칙]
---

# 수집기 프로젝트 규칙 점검 — 2026-09-15

`verify-vault` 스킬로 수집기 프로젝트 범위만 검사한 결과. 규칙 원본은 `_규칙/구역 규칙.yaml`·`_규칙/태그 사전.md`. 링크 그래프 정확도를 위해 전체 Vault를 스캔한 뒤 결과만 수집기 범위로 필터링했다. **노트는 수정하지 않음 — 검사·보고만.**

검사 대상: `프로젝트 노트/`의 수집기 NN 노트·수집기 파생 개념 노트(`개념 - 멱등성`·`빠르게 실패하기`·`되돌릴 수 있는 결정과 없는 결정`), `변경이력/`, `트러블슈팅/`, `수집기 코드분석/`, 수집기 프로젝트 MOC·로드맵·포트폴리오·설계 변경 기록·규칙제안. (KDT·WMS·RAG·Fast-EQA 등 타 구역 제외) — 총 54개 노트.

## ① 요약: **PASS — 이상 없음**

수집기 범위 노트 54개 기준 **FAIL 0건 · WARN 0건**. 2026-09-07 점검의 FAIL 36건·WARN 40건이 모두 해소됐다.

## ② 위반 목록

없음. 항목별 전부 0건:

- frontmatter 없음 / 키 누락(출처·작성일·tags): 0
- 유형 태그 1개 아님: 0
- 영역 태그 3개 이상: 0
- 깨진 위키링크: 0
- 구역 밖 링크: 0
- 비공개(Fast-EQA) 유출: 0
- 파일명 접두사 불일치: 0
- 고아 노트: 0
- 미등록 frontmatter 태그 / 미등록 인라인 태그: 0

## ③ 개념 노드 승격 후보

**없음.** 수집기에서 쓰인 인라인 개념 태그는 `#멱등성`(6)·`#빠른실패`(3)·`#되돌릴수있는가`(3) 세 가지뿐이며, 셋 다 이미 개념 노트(`개념 - 멱등성`·`개념 - 빠르게 실패하기`·`개념 - 되돌릴 수 있는 결정과 없는 결정`)로 승격돼 있고 태그 사전에도 등록돼 있다. 검사기 기계 탐지(2구역+ 등장)도 신규 후보 0.

## 참고: 09-07 대비 해소 경위

지난 점검의 위반은 규칙 쪽과 노트 쪽이 함께 정리되며 사라졌다.

- **유형 태그 30건** → 태그 사전에서 `프로젝트` 유형을 프로젝트 노트 구역까지 확장(2026-09-04). 수집기 06번 이후 노트가 `[프로젝트, sql, python]` 식으로 정리돼 유형 1개 + 영역 태그 형태를 지킨다.
- **미등록 태그(WARN)** → 영역 태그에 `sql`·`python`·`spring`(09-09) 등록. 인라인 표에 `#빠른실패`·`#되돌릴수있는가` 등록(기존 `#멱등성`만 있던 상태 해소).
- **파일명 접두사 6건** → `프로젝트 노트` 구역에 `하위폴더_prefix` 규칙 신설(`변경이력 - `·`트러블슈팅 - `·`코드분석 - `·`WMS 정리 - `). 하위 노트들이 자기 폴더 접두사를 기대값으로 인정받는다.

수집기 노트끼리의 링크 구조(번호 트리·앞뒤 사슬·개념 관문)는 건강하며 고아·구역 이탈 없음.

## 운영 메모

- 이 점검 실행 시 데스크톱 `device_bash` 마운트가 Windows 9월 8일 업데이트로 불가했다. 파일 열람·검사는 스테이징 경로로 우회해 정상 완료. 검사 결과에는 영향 없음.
- 처리 완료 시 마스터가 상단에 `처리: 완료 (날짜)` 한 줄 추가.
