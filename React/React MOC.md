---
출처: Claude 분석
작성일: 2026-09-11
tags: [허브, react]
---

# React MOC

React 학습노트의 허브입니다. 상위 지도는 [[KDT_2026 학습 지도]]. (JavaScript에서 분리 — 원본 코드: `KDT_2026/2026_React`, Vite 프로젝트 `src/example/dayNN`)

## 학습 순서

[[React day01 컴포넌트와 렌더링]] → [[React day02 객체 배열과 map 렌더링]] → [[React day02 컴포넌트 분리와 콜백 props]] → [[React day02 useState와 상태 갱신]] → [[React day02 useEffect와 fetch로 서버 CRUD]] → [[React day02 모달과 컴포넌트 간 상태 전달]] → [[React day03 JSX 스타일링과 이미지 경로]] → [[React day03 폼 제출과 입력값 읽기]] → [[React day03 전화번호부 실습 배열 상태 추가와 삭제]] → [[React day04 React Router 도입과 라우트 정의]] → [[React day05 컴포넌트 생명주기와 useEffect]] → [[React day05 외부 API 호출과 목록 렌더링]] → [[React day05 axios로 백엔드에 POST 보내기]]

## 노트

| 노트 | 핵심 |
| --- | --- |
| [[React day01 컴포넌트와 렌더링]] | Vite 프로젝트 구조(`package.json`·`type: module`·`react`/`react-dom` 분리), `index.html`의 빈 `#root`와 모듈 스크립트 진입점, `createRoot(root).render(<App />)` 3단계, 컴포넌트 = JSX를 반환하는 함수(대문자 이름·`props`·`export default`), 태그로 쓰는 컴포넌트, JSX와 HTML의 차이(`className`·`onClick={}`·자기 닫힘·프래그먼트), 컴포넌트 안에 컴포넌트 조립(헤더·메인·푸터, 내보내지 않는 내부 부품), 함수 선언·화살표·함수 표현식 세 표기, props(매개변수·인수 용어, 부모→자식 읽기 전용 객체, `props.name` vs 구조 분해 `{ name, age }`, JSX 주석 `{/* */}`), 배열 props를 반복문으로 `<li>` 목록 찍기(`key`, `{배열}` 펼침), 이벤트 `onClick={함수}`(카멜케이스·함수를 넘기고 실행하지 않기·인라인 화살표), `useState`와 선언형 렌더링으로 이어지는 자리 |
| [[React day02 객체 배열과 map 렌더링]] | 서버 응답을 가정한 객체 배열(`{ name, imageUrl }`)을 `data.map(...)`으로 돌려 `Profile` 컴포넌트 목록 찍기, 인덱스 하드코딩 → `map` 한 줄로 가는 이유, props만 받는 순수 부품, `key` 선택 기준, 콜백 축약·구조 분해·스프레드 props, 목록 컴포넌트 3겹 나누기, 빈 배열 처리, `useState`+`useEffect`+axios로 이어지는 자리 |
| [[React day02 컴포넌트 분리와 콜백 props]] | 부품을 파일 하나씩으로 나눠 `import`/`export default`로 조립(`./` 상대 경로, 파일명 = 컴포넌트명), 함수 선언 vs `const` 화살표 표기, 함수를 props로 내려보내고 자식이 호출해 부모에 알리는 콜백 props(데이터는 내려가고 이벤트는 올라감), 콜백은 넘길 때 실행하지 않기(`cal(plus)` vs `cal(plus(3,5))`), `<a>`의 GET 이동을 `preventDefault`로 막는 이유(CSR 깜빡임), props·구조 분해 용어 정리, `on`/`handle` 이름 관례, 클릭한 항목 id 올려보내기, 상태 끌어올리기·React Router·Context로 이어지는 자리 |
| [[React day02 useState와 상태 갱신]] | 전역·지역 변수는 늘어나도 화면이 안 바뀌는 이유(`return`이 한 번만 실행), `useState(초기값)` → `[값, setXXX]` 구조 분해, `setXXX`가 컴포넌트를 재호출해 화면을 새로고침하고 상태값은 유지되는 원리, 배열·객체 상태는 **주소값이 바뀌어야** 재렌더링(`push`만으로는 X → `[...array]`/`{...obj}` 스프레드 복사), `mode` 상태로 `FrontComp`/`BackComp`를 골라 그리는 조건부 렌더링(JSX를 변수에 담아 `if`로 갈아끼우기), 콜백 props로 자식이 부모 상태를 바꾸는 상태 끌어올리기(`setMode` 감싸 넘기기 vs 그대로 넘기기), 함수형 업데이트·불변성·Virtual DOM·`useEffect`로 이어지는 자리 |
| [[React day02 useEffect와 fetch로 서버 CRUD]] | 실습 2(통합 제품 관리)의 `CategoryManager`로 보는 서버 통신 기본형 — DB 컬럼명 = JSON 키 = 프로퍼티명(`totalPractice.sql`), 목록 초기값 `[]`, `async/await` + `fetch` → `res.ok` → `res.json()` → `Array.isArray` 방어, `useEffect(…, [])`로 첫 렌더링 뒤 한 번만 조회(본문에서 부르면 무한 반복), POST(`Content-Type: application/json` + `JSON.stringify`) 뒤 재조회, DELETE는 쿼리 스트링 `?cno=`, `window.confirm`, 제어 컴포넌트(`value`/`onChange`, `setName('')`로 비우기), `<form onSubmit>` + `preventDefault`, 빈 목록 삼항과 `key={cat.cno}`, 오류·로딩 상태, 주소 상수화, CORS·StrictMode 이중 실행·AXIOS로 이어지는 자리 |
| [[React day02 모달과 컴포넌트 간 상태 전달]] | `ProductManager`·`ReviewManager` — 상태 7개(목록·모달 boolean·모달+대상 객체·폼 객체), 객체 폼 상태의 스프레드 갱신(`{ ...form, name }`), `<select value>` 제어와 옵셔널 체이닝, 문자열 → `Number` 변환, 모달 = `상태 && <JSX>` + `position: fixed` 배경, 인라인 `style` 객체 두 겹, 자식→부모 콜백 `oncategoryupdated(list)`로 두 벌의 목록 맞추기, `product`·`onclose`로 자식이 모달 껍데기까지 그리는 패턴, `useEffect` 의존성 `[product?.bno]`, 수정 모달(폼 채우기 + PUT 본문에 키), `repeat`로 별점, `onChange` 합치기·`Modal` 컴포넌트와 `children`·상태 끌어올리기 한계 → Context·커스텀 훅·포털로 이어지는 자리 |
| [[React day03 JSX 스타일링과 이미지 경로]] | `exam1.jsx` — 인라인 `style`은 문자열이 아니라 객체(중괄호 두 겹, `backgroundColor` 카멜케이스, 단위까지 문자열, 컴포넌트 바깥 상수로 재사용), CSS 파일을 `import "../../index.css"`로 끌어와 `className`·`id`로 붙이기(`class` 예약어, import한 CSS는 전역), 이미지 세 경로(`public/` 절대 경로 vs `src/assets` `import`로 해시 경로 vs 외부 URL), `main.jsx`에서 진입 컴포넌트 갈아끼우기, 조건부 `className`·스타일 객체 스프레드·인라인과 CSS 파일 역할 나누기, CSS Modules·Tailwind·styled-components로 이어지는 자리 |
| [[React day03 폼 제출과 입력값 읽기]] | `exam2.jsx` — `<form onSubmit>`은 버튼이 아니라 폼의 이벤트, `preventDefault`로 GET 이동 차단, `event.target`이 폼 자신이라 `event.target.name속성.value`로 상태 없이 입력값 읽기(`name` 필수), 상태 없는 `WriteForm`이 값을 콜백 props로 올리고 부모가 검증·`message` 상태 갱신, `<input>` 세 개 비교(비제어 · 고정 `value`는 타이핑 불가 · `value`+`onChange` 제어), 제어 vs 비제어 고르는 기준, `FormData`·`reset()`·`trim()`·`defaultValue`, `useRef`·React Hook Form으로 이어지는 자리 |
| [[React day03 전화번호부 실습 배열 상태 추가와 삭제]] | `pracitce/practice.jsx` — 비제어 `WriteForm`(입력 3개, `placeholder`, `e.target.<name>.value`)이 콜백으로 값을 올리고 부모 `Component3`가 배열 상태를 맡는 전화번호부, 추가 `[...array, { name, number, age }]`(스프레드 + 단축 프로퍼티), 삭제 `splice` 뒤 `[...array]`로 주소값 갱신, `map`의 두 번째 매개변수 `index`를 삭제 버튼에 화살표로 감싸 넘기기, `array.length`로 파생값 표시(상태로 두지 않기), 실습 폴더의 `./index.css` `import`, `filter`로 삭제·`key`와 고유 `id`·`reset()`·`every`+`trim()` 검증·HTML 검증 속성, `useReducer`·localStorage·항목 컴포넌트 분리로 이어지는 자리 |
| [[React day04 React Router 도입과 라우트 정의]] | `exam1.jsx`·`App.jsx`·`Home.jsx`·`LayoutIndex.jsx`·`NotFound.jsx`·`TopNavi.jsx`·`RouterHooks.jsx`·`CommonLayout.jsx`·`practice/*`·`main.jsx` — `react-router-dom` 설치(`package.json` 의존성, `react`/`react-dom`처럼 별도 패키지), 최초 렌더링 컴포넌트를 `<BrowserRouter>`로 한 번 감싸기(주소 감시 컨텍스트, 앱에 하나), `<Routes>` 안에 `<Route path="/경로" element={<컴포넌트 />} />`로 경로 표 선언(`path`는 도메인 이후 주소, `element`에는 JSX 태그), 페이지 = `Route`에 꽂힌 보통 컴포넌트, 세 파일이 맞물리는 순서(라우터 → 경로 표 → 화면)와 `Routes` 자리만 교체되는 SPA 라우팅, 공통 헤더·푸터는 `Routes` 바깥, 라우트 표 확장(`index` 라우트 `LayoutIndex`·상대 경로 `intro`·`path="*"` → `NotFound` + `Link to="/"`), `TopNavi`의 `<a>`(새로고침 있음) vs `NavLink`(`active` 클래스) vs `Link` 비교, `main.jsx`에서 day04 `App` 교체, `Home` 라우트 아래 `intro`·`index`·`router` 중첩(상대 경로가 부모 뒤에 붙음, 부모에 `Outlet` 자리 필요), `RouterHooks`의 `useLocation`(`pathname`·`search` 읽기 전용)과 `useSearchParams`(`get`으로 문자열 꺼내기, `setSearchParams` 객체가 쿼리 전체를 교체)로 mode·pageNum을 주소에 두기, 훅은 컴포넌트 최상위에서, `Home`에 `TopNavi`+`<Outlet />`을 넣어 부모 라우트를 공통 레이아웃으로(`path="/"`, 자식 `index`·`intro`·`router`가 `Outlet` 자리에), `practice/`의 사이드 네비 팀 소개 실습(`flex` 두 칸 `wrap`, `SideNav`의 `NavLink`+`className`, 팀원별 자식 라우트를 `App3`에 한 줄씩), `useNavigate`·`:param`+`useParams`·`setSearchParams` 함수형 갱신, 중첩 라우트+`Outlet`·레이아웃 라우트·배포 시 `index.html` 폴백·데이터 라우터·`React.lazy`로 이어지는 자리 |
| [[React day05 컴포넌트 생명주기와 useEffect]] | `Lifecycle.jsx`·`TopNav.jsx`·`App.jsx`·`exam.md` — 재렌더링의 방아쇠는 state 변경과 props 변경 둘뿐(함수는 `return` 1회라 화면 갱신 = 함수 재실행), `useEffect` 세 형태와 의존성 배열(`[]` 마운트 1회 · `[state]` 그 값이 바뀔 때 · 배열 생략은 매 렌더링이라 무한 루프 위험), 콜백이 반환하는 정리(cleanup) 함수 = 언마운트 + 다음 effect 직전(타이머·리스너·요청 취소 짝 맞추기), `console.log`로 확인한 실행 순서(함수 본문 → 화면 그리기 → effect, 재실행 때는 정리가 먼저), 세 형태를 주석으로 남겨 두고 최종적으로 고른 `[leftCount]`(우측이동은 렌더링만·좌측이동은 effect까지 — 재렌더링과 effect 실행은 별개의 사건), `MoveBox`의 `position`·`leftCount` 상태와 인라인 스타일 문자열 조립(템플릿 리터럴 백틱, `position: relative`와 `left`의 짝), `onClick={함수}` 클릭 → `setXXX` → 재렌더링 흐름, `props.initPosition`을 `useState` 초기값으로(첫 렌더링에서만 반영), `TopNav`의 `NavLink` 세 갈래(생명주기·내부통신·외부통신)로 day05 목차 예고, `App4`는 공통 헤더를 `Routes` 바깥에 두고 `main.jsx` 진입 컴포넌트 교체, 함수형 업데이트 `(prev) => prev + 1`와 배칭, 렌더링 중에 하면 안 되는 일(요청·DOM 조작·상태 변경), 클래스 생명주기 메서드 대응표, `useLayoutEffect`·`useRef`·CSS transition·커스텀 훅·React Query로 이어지는 자리 |
| [[React day05 외부 API 호출과 목록 렌더링]] | `ExternalApiFetcher.jsx`·`main.jsx` — 남의 서버(randomuser.me)를 부르는 통신 컴포넌트의 기본 뼈대(`useState` 초기값을 응답 모양대로 `{ results: [] }` · `useEffect(…, [])`로 마운트 1회 요청 · `map` 렌더링), 화면이 먼저 그려지고 데이터가 나중에 오는 두 번의 실행 순서와 초기값이 그 첫 한 번을 버티게 하는 이유, 외부 API의 중첩 JSON을 `console.log`로 먼저 읽고 `data.name.first`·`data.picture.thumbnail`로 꺼내기(자체 서버의 평평한 객체와 다른 점), `map` 결과를 `trTag` 변수에 담아 `<tbody>`에 꽂기와 `key={data.login.md5}`(인덱스 대신 응답의 고유값), 목록 항목 링크에서 `e.preventDefault()` + 인라인 화살표로 감싼 콜백 props로 **원본 객체 전체**를 부모에 올려보내기, 데이터 담당(`RandomUser`)과 동작 담당(`ExternalApiFetcher`)의 역할 분리·`on...` 네이밍·백틱 템플릿 리터럴, `main.jsx` 진입 컴포넌트 교체와 `BrowserRouter` 유지, `fetch` vs `axios` 비교표(`res.json()` vs `res.data`, 섞지 않기), `useEffect` 콜백을 `async`로 만들지 않고 안쪽에 함수를 선언해 호출하는 표준형, 로딩·오류·데이터 세 상태 한 벌과 `finally`, 쿼리 스트링과 `[count]` 의존성, 선택 항목을 상태로 올려 `상태 && <JSX>` 상세 화면, 옵셔널 체이닝 `?.`/`??`, CORS와 출처·요청 취소(`AbortController`)와 경쟁 상태, 커스텀 훅 `useFetch`·React Query·API 키와 환경 변수로 이어지는 자리 |
| [[React day05 axios로 백엔드에 POST 보내기]] | `day04/practice/신승민.jsx`·`index.css` — day04 사이드 네비 실습의 내 페이지에 얹은 제품 등록 폼, 입력 칸마다 `useState("")` 하나씩 두는 제어 컴포넌트(초기값을 빈 문자열로 두는 이유), `axios.post(주소, 객체)` 한 줄이 `fetch`의 `method`·`Content-Type`·`JSON.stringify` 세 줄을 대신하는 것과 `axios.HTTP메소드명` 공통 형태, 단축 프로퍼티 `{ name, price, cno }`로 만든 본문과 "상태 이름 = JSON 키 = 서버 DTO 필드명" 사슬, `<form>` 없이 `<input type="submit">`의 `onClick={함수}`로 보내기(괄호 붙이면 렌더링 때 실행), 페이지 컴포넌트(`Seung`) 안에 내부 부품(`ProductPrint`)을 두는 구조, 표 안 폼을 `flex` 두 겹(`.inputWrap` `column`)으로 세로 배치하기와 `table` 계열에 `display: flex`를 주면 표 성질이 사라지는 점·전역 CSS의 클래스 이름 충돌, `onSubmit`+`preventDefault`로 엔터 제출과 `setXXX("")` 초기화, 숫자 필드 `Number()` 변환, `try/catch`와 `response.status`·`data` 확인·중복 전송 `disabled`, 등록 후 재조회, 주소 상수화와 `.env`(`VITE_`)·`axios.create` 인스턴스·인터셉터, 객체 폼 상태와 계산된 프로퍼티 `[e.target.name]`, 프론트 검증과 백엔드 `@Valid`의 역할 차이, CORS·개발용 터널 주소, `FormData` 업로드·낙관적 업데이트·React Hook Form·`useMutation`으로 이어지는 자리 |

## 앞선 갈래

순수 JS·DOM 조작(2026_FE)은 JavaScript MOC 관할이다. React 수업의 선행 흐름은 JS day11 DOM 조작 → JS day14 게시판 CRUD → 여기(평문 안내 — 구역 간 직접 링크는 걸지 않는다).
