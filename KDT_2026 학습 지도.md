---
출처: Claude 분석
원본: C:\Users\harry\Desktop\KDT_2026
작성일: 2026-08-10
tags: [허브]
---

# KDT_2026 학습 지도

> VSCode 워크스페이스 `KDT_2026`에서 배운 내용을 **day 단위**로 쪼개 언어별 폴더에 정리한 전체 지도입니다. 원본 코드는 읽기 전용으로만 다뤘습니다.

## 저장소 개요

```
KDT_2026/
├── 2026B_BE/src/     Java day01~16 + database(SQL)
├── 2026B_Spring/     Spring Boot 프로젝트 (springweb)
└── 2026_FE/          HTML·CSS·JS day02~15 + Note + LevelUP + extra_assignment
```

| 구분 | 수량 |
| --- | --- |
| HTML | 77 |
| JS | 46 |
| CSS | 34 |
| Java | 24 |
| SQL | 6 |
| 노트(txt·무확장자) | 18 |

상위: [[Vault 홈]]

## 언어별 허브

- [[Java MOC]] — 백엔드 day01~16 + 스프링
- [[JavaScript MOC]] — 프론트 day02~14
- [[HTML MOC]] — day02, day04, day15
- [[CSS MOC]] — day05~day15
- [[CS 이론 MOC]] — SQL·데이터베이스

## 개념 노트 (가로 관문)

여러 day 노트에 걸쳐 반복되는 개념을 모아둔 관문 노트다. 각 지점에서 들어오고 여기서 다른 지점으로 나간다.

- [[개념 - CRUD]] — 게시판 CRUD의 공통 뼈대 (JS day12·day14, Java day06·day09)
- [[개념 - 싱글톤]] — 저장소를 하나로 두는 패턴 (Java day08·day09·day11·day12)
- [[Repository Pattern]] — 데이터 계층 분리·DAO·DI (사용자 원본)

## 날짜별 전체 색인

### 2026B_BE (백엔드 10일)

| day | 주제 | 노트 |
| --- | --- | --- |
| day01 | 자바 구조·자료형·입출력 | [[Java day01 자바 구조와 자료형]] |
| day02 | 타입 변환 | [[Java day02 타입 변환]] |
| day03 | 연산자 | [[Java day03 연산자]] |
| day04 | 제어문·배열 | [[Java day04 제어문과 배열]] |
| day05 | 클래스·인스턴스 | [[Java day05 클래스와 인스턴스]] |
| day06 | 생성자·콘솔 게시판 | [[Java day06 생성자와 콘솔 게시판]] |
| day07 | 메소드·미니프로젝트 | [[Java day07 메소드와 미니프로젝트]] |
| day08 | 접근제한자·캡슐화·static | [[Java day08 접근제한자와 static]] |
| day09 | ArrayList | [[Java day09 ArrayList]] |
| day09 | **MVC 종합예제** (View/Controller/DAO/DTO) | [[Java day09 MVC 종합예제]] |
| day10 | 상속·다형성·오버라이딩 | [[Java day10 상속과 다형성]] |
| day11 | 인터페이스·implements·다중 구현·익명 구현체 | [[Java day11 인터페이스]] |
| day11 | **종합예제** (인터페이스 DAO·제네릭·싱글톤·BaseTime 상속) | [[Java day11 종합예제 인터페이스 DAO]] |
| day12 | 예외 처리·JDBC 연결·질의 | [[Java day12 예외 처리와 JDBC]] |
| day12 | **종합예제** (BaseDao 상속·JDBC 연동·싱글톤 MVC 배선·CRUD 완성) | [[Java day12 종합예제 JDBC DAO]] |
| day13 | Object 최상위 클래스·toString·equals·hashCode·Class·리플렉션, 래퍼 클래스·타입 변환·날짜/시간(java.time)·String 클래스·Random·UUID, 구분자 문자열 실습(파싱·요금 계산) | [[Java day13 Object 클래스와 리플렉션]] |
| day14 | 제네릭 — 타입 파라미터·다중/중첩 타입·제네릭 메소드·상한 제약(`extends`), 컬렉션 프레임워크 — List·Set·Map, 인터페이스 선언과 다형성, 리스트 순회, 구현체 구조 차이, Set의 중복 제거와 Iterator·TreeSet, 제네릭 클래스 설계와 와일드카드 `<?>` 컬렉션 | [[Java day14 제네릭]] |
| day15 | Map과 HashMap — 컬렉션 세 갈래 재정리, 엔트리(키·값 쌍)와 JSON 대응, `put` 의 키 중복 덮어쓰기, `get`·`containsKey`·`keySet`·`values`·`remove`, 인덱스가 없어 `keySet()` 을 거치는 순회, `Stack`(LIFO)·`Queue`(FIFO)와 구현체 선택, 단일 스레드와 `Thread.sleep`, 프로세스와 스레드, 멀티스레드 구현 세 가지(익명 구현체·`Runnable` 구현·`Thread` 상속)와 `start()`·`run()` 의 차이, 시계·타이머 스레드와 플래그로 흐름 끄기, main 스레드가 입력으로 다른 흐름을 제어하기 | [[Java day15 Map과 HashMap]] |
| day16 | 스레드 동기화와 스레드 풀 — 멀티스레드 웹서버 구조, 동기화(락·대기)와 비동기화, 같은 객체를 참조하는 공유 자원과 경쟁 상태, `Thread.sleep` 으로 어긋남 재현, `synchronized` 메소드와 객체 단위의 락, 안전과 속도의 맞바꿈, 스레드 풀로 과부하 막기 — 고정 크기 풀·선입선출 작업 큐·`Runnable` 작업 객체·`submit()`·풀 상태 조회·`shutdown()` | [[Java day16 스레드 동기화]] |
| Spring | **Spring Boot 프로젝트 생성** — `src/main`·`src/test` 고정 구조, `build.gradle` 의 플러그인·자바 21 툴체인·의존성 선언, 스타터(`spring-boot-starter-webmvc`)와 버전 자동 관리, 내장 톰캣과 실행 가능한 jar, 그레이들 래퍼, `@SpringBootApplication` 세 애노테이션과 컴포넌트 스캔, `SpringApplication.run()`, `application.properties` 로 설정 분리, `@SpringBootTest`·`contextLoads`, 컨트롤러·의존성 주입·싱글톤 빈과 무상태 | [[Spring Boot 프로젝트 생성(분석)]] |
| Spring day01 | **서블릿과 HTTP 메소드** — 자바 클래스에 HTTP를 붙이는 규격과 서블릿 컨테이너(톰캣), `jakarta.servlet` 패키지, `extends HttpServlet` 상속, 생명주기 `init`·`service`·`destroy` 와 호출 시점, `service` 가 `doXXX` 를 갈라 부르는 구조, `doGet`·`doPost`·`doPut`·`doDelete` 와 CRUD·주소+방식 분기, `@WebServlet` 매핑, `HttpServletRequest`·`HttpServletResponse` 로 값 꺼내기와 응답 쓰기, 상태 코드·포워드/리다이렉트·세션·스코프·필터, 단일 인스턴스와 멀티스레드 안전성, 프론트 컨트롤러(`DispatcherServlet`)로 이어지는 자리 | [[Spring day01 서블릿과 HTTP 메소드]] |
| Spring day02 | **스프링 부트 실행과 계층 이식, 웹 CRUD 잇기, 정적 화면 붙이기** — 프레임워크와 라이브러리의 차이(제어의 역전), 애노테이션이 코드에 의미를 붙이는 방식과 주석과의 차이, `@SpringBootApplication` 세 애노테이션·내장 톰캣 자동 세팅·컴포넌트 스캔 범위, `SpringApplication.run(클래스.class)` 와 `ApplicationContext`, 8080 포트·`localhost`·`127.0.0.1`·포트 충돌과 `server.port`, `main` 이 여럿일 때 `build.gradle` 의 `springBoot { mainClass }` 로 진입점 고르기, 패키지로 나눈 Controller·Model(Dao·Dto) 계층과 View 자리를 맡는 브라우저, 서블릿 기능을 상속 대신 표시로 받기·`@Controller` 와 `@RestController` 의 갈림과 반환값이 데이터로 읽히는 자리, 객체·컬렉션이 JSON으로 바뀌는 과정과 getter의 역할, HTTP Content-Type과 DTO의 자리, `@PostMapping`·`@GetMapping`·`@PutMapping`·`@DeleteMapping` 넷과 CRUD 짝짓기, 커맨드 객체 바인딩(요청 파라미터 이름 ↔ 필드 이름)과 값 하나만 받는 매개변수 바인딩·`@RequestParam`, `BaseDao` 상속으로 JDBC 연동 공통화·`protected` 접근 범위·`Class.forName` 드라이버 로드·생성자 연동, 싱글톤과 상속을 겹쳐 쓴 `BoardDao`, SQL 다섯 단계·`?` 바인딩과 SQL 인젝션·`executeUpdate` 반환값으로 성공·0건 판정·`SQLException` 검사 예외, `executeQuery` 와 `ResultSet` 커서를 `while (rs.next())` 로 훑어 DTO 목록 만들기·줄마다 새 객체·컬럼을 이름으로 꺼내기·빈 리스트 반환, `update`·`delete` 의 `where` 조건절과 `?` 인덱스 순서, DB 컬럼과 짝을 이루는 DTO·기본 생성자가 필요한 자리, `sample.sql` 의 `AUTO_INCREMENT`·`PRIMARY KEY`·`DROP IF EXISTS`, 연결 정보를 `application.properties` 로 분리, `DataSource` 와 커넥션 풀, 스프링 빈과 의존성 주입, `@ResponseBody`·`ResponseEntity`·로거·`@RestControllerAdvice`, REST 자원 중심 주소와 `@PathVariable`·`@RequestBody`, `fetch`·`curl` 로 GET 외의 요청 보내 보기, Service 계층·`JdbcTemplate`·JPA·트랜잭션, 같은 계층 한 벌을 두 번째 도메인(대기명단)에 다시 쓰기 — 표가 늘면 Dto·Dao·Controller만 늘고 `BaseDao`·`AppStart` 는 그대로, 제약을 컬럼 뒤에 붙이는 표기와 `constraint` 표기·`NOT NULL`·`UNIQUE`·백틱 식별자, DB 컬럼 이름과 자바 필드 이름이 다를 때의 짝짓기(`ResultSet` 은 컬럼 이름·요청 바인딩은 프로퍼티 이름)와 자바빈 프로퍼티 규약·스네이크↔카멜·앞 두 글자가 대문자면 그대로 두는 예외와 폼 바인딩·JSON 변환의 이름 규칙이 갈리는 자리·`@JsonProperty`, 필드를 `private` 으로 닫아 통로를 하나로 묶기, 대리키와 자연키·조건절에 자연키를 쓸 때 `executeUpdate` 가 2 이상을 돌려주는 상황, 컨트롤러가 여럿일 때 주소 충돌과 `@RequestMapping` 으로 공통 앞머리 두기, `src/main/resources/static` 에 둔 HTML을 내장 톰캣이 매핑 없이 내보내는 정적 리소스 규칙·폴더 구조가 곧 주소가 되는 점·`index.html` 이 기본 문서로 취급되는 이름, `<!doctype>`·`charset`·viewport meta와 `<head>`·`<body>` 의 갈림, 입력칸·`<thead>`/`<tbody>` 표·수정·삭제 버튼이 CRUD 매핑 넷과 짝을 이루는 구조, `file://` 로 여는 것과 `http://localhost:8080` 으로 여는 것의 차이와 같은 출처, 빌드 결과(`build/resources/main`)로 복사되는 자리, `fetch` 로 화면과 API 잇기와 JSON 프로퍼티 이름이 화면에서 쓰는 이름이 되는 자리, 정적 파일 주소와 컨트롤러 매핑이 겹칠 때의 우선순위·`/api` 앞머리로 갈라 두기, 템플릿 엔진(Thymeleaf)·`resources/templates`·`Model` 과 서버가 화면을 그리는 방식의 갈림, 화면에 `class`·`id` 식별자를 달고 `onclick` 으로 함수 걸기·`<script>` 를 body 끝에 두는 이유와 실행 순서·CDN 주소와 상대 경로, axios로 HTTP 메소드를 그대로 부르기와 `@GetMapping`~`@DeleteMapping` 넷과의 짝, 비동기와 Promise·`async`/`await` 가 짝으로 다니는 이유, 응답 한 벌(`status`·`headers`·`data`)과 본문을 `.data` 로 꺼내기·`ArrayList<BoardDto>` 가 JSON 배열을 거쳐 자바스크립트 배열이 되는 왕복, `querySelector` 로 채울 자리 잡기·반복문으로 모은 마크업 문자열을 마지막에 한 번만 `innerHTML` 로 넣기·템플릿 리터럴, DB 컬럼→필드→getter→JSON 키→화면 코드로 이름 하나가 관통하는 자리, 파일 끝에서 함수를 바로 불러 페이지 열릴 때 목록 그리기, axios와 `fetch` 의 갈림(본문 꺼내기·실패 응답 처리·공통 설정), 절대주소와 상대주소·출처(origin)와 CORS·`@CrossOrigin`·`addCorsMappings`, 등록·수정을 화면에 붙이기 — `.value` 로 입력칸 값 꺼내기·값을 쿼리 스트링에 실어 POST·PUT 보내기·커맨드 객체 바인딩이 주소와 본문을 가리지 않는 점·`response.data` 로 `boolean` 판정하기·성공 후 조회 함수를 재호출해 표만 다시 그리기와 `location.reload()` 의 갈림·`alert`·`confirm`·`prompt` 가 각각 돌려주는 값·수정 함수가 대상 번호를 매개변수로 받는 구조와 표를 그릴 때 `onclick` 에 값을 박아 잇기·주소창으로는 못 보내던 PUT을 axios로 보내기·세 함수가 같은 틀을 반복하는 모양, 값을 쿼리 스트링에 실을 때와 본문에 실을 때의 갈림(기록에 남는 자리·길이·특수문자)·`encodeURIComponent`·`axios.post(주소, 객체)` 와 `@RequestBody`, 삭제까지 화면에 붙여 CRUD 네 갈래 완성하기 — `axios.delete` 와 매핑 넷의 짝·값을 모으는 단계 없이 대상만 넘기는 구조·되돌릴 수 없는 요청 앞의 `confirm` 과 조기 반환·`boolean` 하나로는 0건과 오류를 갈라 보이지 못하는 자리, 같은 화면 한 벌을 두 번째 도메인(대기명단)에 다시 쓰기 — 화면 파일을 도메인마다 나누기와 `static/` 폴더 구조가 곧 주소가 되는 규칙의 반복·폴더 주소로 열리는 `index.html` 과 이름까지 적어야 닿는 파일의 갈림·마크업 골격은 같고 이름만 다른 자리들·채울 자리를 가리키는 클래스 이름을 화면마다 통일해 두기·함수 이름 앞머리를 도메인으로 나누기와 전역 이름이 겹치는 자리·수정·삭제 대상을 `onclick` 으로 넘길 때와 `prompt` 로 물어볼 때의 갈림(대리키는 화면이 들고 있다 넘기고 자연키는 사람에게 묻는다)·템플릿 리터럴에 문자열 값을 박을 때 따옴표로 감싸기·JSON 키가 곧 화면에서 꺼내는 이름이 되는 자리와 응답 본문을 직접 열어 확인하기·값이 `undefined` 로 찍힐 때 요청 실패인지 이름 불일치인지 갈라 보기·`.value` 가 언제나 문자열이라는 점과 요청 파라미터의 타입 변환·빈 값이 DB 제약보다 앞에서 걸리는 자리와 `<input type="number">`·도메인이 늘 때 손대는 다섯 파일과 그 되풀이를 줄이는 두 방향 | [[Spring day02 스프링 부트 실행과 계층 이식]] |
| Spring day03 | **애노테이션과 리플렉션 — 스프링이 도는 원리** — 애노테이션이 주석과 갈리는 지점(사람이 읽는 메모 vs 기계가 읽는 표시)과 컴파일 후에도 남는 성질, `@Override` 로 재정의를 컴파일러에게 검사시키기와 재정의된 메소드가 불리는 확인·`super` 로 부모 메소드 부르기, `@Deprecated` 로 사용 권장하지 않음을 알리기와 실행은 되지만 경고가 나는 자리, `@SuppressWarnings` 까지 표준 애노테이션 셋이 모두 컴파일러에게 말을 거는 표시라는 정리, `@interface` 로 애노테이션 직접 정의하기, 메타 애노테이션 `@Retention` 과 `RetentionPolicy` 세 단계(`SOURCE`·`CLASS`·`RUNTIME`)·누가 언제 읽느냐가 값을 정한다는 점·실행 중 읽으려면 `RUNTIME` 이어야 하는 이유, `@Target` 과 `ElementType`(`TYPE`·`METHOD`·`FIELD`·`PARAMETER` 등)으로 붙일 자리 제한하기·여러 자리를 배열로 적기·잘못 붙이는 것을 컴파일 시점에 막는 값어치, 애노테이션 속성이 추상메소드 모양인 이유와 `default` 유무로 갈리는 필수·선택·속성 타입의 제한·`value` 하나만 쓸 때 이름 생략 규칙과 `@GetMapping("/board")` 표기가 여기 얹히는 자리, 정의한 애노테이션을 메소드에 달고 생략한 속성이 기본값으로 채워지는 자리, `java.lang.annotation`(만드는 축)과 `java.lang.reflect`(읽는 축)의 갈림, `클래스.class` 리터럴로 `Class` 얻기와 `getClass()`·`Class.forName()` 과의 갈림·제네릭 `Class<T>` 로 타입 남기기, `getMethod(이름)` 으로 `Method` 꺼내기와 `getDeclaredMethod` 와의 범위 차이(`public`·상속 포함 vs 선언된 것 전부)·오버로딩된 메소드를 매개변수 타입까지 넘겨 고르기, `getAnnotation(애노테이션.class)` 로 표시 읽기와 `null` 이 나올 수 있는 자리·`isAnnotationPresent` 로 먼저 확인하기, 속성 이름 그대로 메소드처럼 값 꺼내기, `getDeclaredConstructor().newInstance()` 로 `new` 없이 객체 만들기와 기본 생성자가 필요한 이유·DTO에 기본 생성자를 남겨 두던 자리와의 연결, `method.invoke(대상)` 으로 이름을 코드에 박지 않고 메소드 실행하기·`static` 이면 `null`·매개변수를 뒤에 잇기, `setAccessible(true)` 로 `private` 을 여는 통로와 접근제한자가 물리적 잠금은 아니라는 점, 리플렉션 예외 계열(`NoSuchMethodException`·`IllegalAccessException`·`InstantiationException`·`InvocationTargetException`)과 `getCause()` 로 진짜 원인 꺼내기, `getDeclaredMethods()` 로 전부 훑어 표시된 것만 골라 실행하기 — 매핑을 추가해도 읽는 쪽을 안 고쳐도 되는 이유, 애노테이션은 스스로 아무 일도 하지 않고 읽는 쪽이 있어야 동작이 된다는 점과 안 먹힐 때 컴포넌트 스캔 범위를 먼저 보는 자리, 정의→주입→읽기 세 단계가 스프링의 컴포넌트 스캔·주소 표 만들기·요청 시 `invoke` 와 같은 구조라는 정리, 메타 애노테이션과 합성 애노테이션(`@RestController`=`@Controller`+`@ResponseBody`, `@GetMapping`=`@RequestMapping(GET)`), AOP와 프록시(`@Transactional` 이 앞뒤로 끼어드는 원리와 자기 호출에 안 먹히는 제약), 컴파일 시점에 읽는 갈래인 애노테이션 프로세서와 Lombok, 리플렉션의 비용(느림·컴파일 검사 부재·리팩터링 취약)과 시작할 때 한 번 읽어 표로 만들어 두는 대응, **롬복으로 남이 만든 애노테이션 가져다 쓰기** — `build.gradle` 에 의존성 추가하기와 `compileOnly`(실행 배포본에서 빠짐)·`annotationProcessor`(컴파일 중 처리기 등록) 두 줄의 뜻, 컴파일 시점에 코드를 생성하는 갈래와 실행 중 리플렉션으로 읽는 갈래의 대비·`.java` 에는 없고 `.class` 에는 있는 메소드·실행 중 부담이 없는 이유, `@NoArgsConstructor`·`@AllArgsConstructor`·`@RequiredArgsConstructor` 로 생성자 만들기와 셋의 갈림·생성자를 직접 정의하면 기본 생성자가 사라지는 규칙이 다시 걸리는 자리·프레임워크가 객체를 만드는 통로가 기본 생성자라는 점, `@Getter`·`@Setter` 로 캡슐화 메소드 자동 생성과 멤버변수가 늘 때 메소드가 따라오는 구조·필드 하나에만 붙이기, `@ToString` 과 `toString()` 재정의의 연결·`클래스명@해시값` 이 찍히던 자리, `@Data` 가 자주 쓰는 것을 묶은 애노테이션이라는 점과 그 구성·합성 애노테이션과 같은 발상, `@EqualsAndHashCode` 로 주소 비교를 값 비교로 바꾸기와 `HashMap`·`HashSet` 에서 둘이 짝으로 맞아야 하는 이유, `exclude`·`of` 로 훑을 필드 고르기와 순환 참조·로그에 남으면 곤란한 값 빼기·`callSuper`, 생성된 코드를 디컴파일·Outline·리플렉션으로 확인하는 통로, 상황별로 붙일 애노테이션 고르기와 `@Setter` 를 습관적으로 붙이지 않는 편이 안전한 이유, `final` + `@RequiredArgsConstructor` 로 만드는 생성자 주입과 필드 주입과의 갈림, MapStruct·Querydsl 같은 코드 생성 계열, **`@Builder` 로 객체 조립하기** — 생성자와 갈리는 지점(순서 무관·선택적 대입·위치가 아니라 이름으로 값을 넣는 것)·`Student.builder()` 가 `static` 이라 객체 없이 불리는 자리·메소드 체이닝이 이어지는 원리와 `build()` 에서 객체가 만들어지는 시점·생략한 멤버가 기본값이 되는 점·값을 빠뜨려도 컴파일이 막지 않는 약점과 `@Builder.Default`·`@NoArgsConstructor` 와 함께 붙여 두는 이유·빌더와 생성자 중 무엇을 고를지의 기준, **객체를 누가 만드는가(IOC/DI)** — 전통 방식의 `new` 와 호출마다 객체가 쌓이는 점·쓰는 쪽에 구현 클래스 이름이 박혀 교체가 막히는 결합도 문제, 손으로 만드는 싱글톤의 세 줄(`private` 생성자로 `new` 막기·`private static final` 인스턴스·`public static getInstance()` 창구)과 `static`·`private`·`final` 이 맞물리는 자리·클래스마다 되풀이되는 부담과 결합도는 그대로 남는 한계·이른 초기화와 늦은 초기화·홀더·`enum` 방식의 갈림과 늦은 초기화에서 스레드가 끼어드는 문제, `@Component` 로 스프링 컨테이너에 빈 자동 등록하기와 정의→읽기→생성 세 단계가 앞의 리플렉션 실습과 같은 구조라는 정리·`@Component` 가 `RUNTIME`·`TYPE` 일 수밖에 없는 짐작·빈(Bean)이라는 이름, 세 방식을 만드는 주체·개수·되풀이 코드·박히는 이름으로 갈라 보기, IOC(제어의 역전)로 객체 생성 판단이 컨테이너로 넘어가는 것과 DI(의존성 주입)로 필요한 것을 받는 것의 짝, 인터페이스 타입으로 받아 두면 구현 교체가 설정 문제로 줄어드는 자리, 빈이 기본적으로 하나로 공유되므로 멤버변수에 상태를 담으면 요청끼리 덮어쓰는 문제와 빈은 기능을 담는 자리라는 정리, 빈 스코프(`singleton`·`prototype`·`request`·`session`)와 `ApplicationContext`·`@PostConstruct`·`@PreDestroy`·`@Primary`·`@Qualifier`, **앞의 표시들을 실제 요청 처리에 다시 조립하기** — day03 패키지에 진입점을 두고 `@SpringBootApplication` 으로 내장 톰캣과 IOC/DI를 함께 켜기·컴포넌트 스캔 범위가 진입점의 패키지와 하위로 정해지므로 어느 진입점을 띄우느냐에 따라 등록되는 빈이 갈리는 자리, `@Controller` 가 `@Component` 를 품고 그 위에 HTTP 요청을 받는 자리를 얹는 구조와 서블릿을 물려받던 자리를 표시 한 줄이 대신하는 점·계층별 이름(`@Controller`·`@Service`·`@Repository`)이 갈려 있는 이유, `@GetMapping` 으로 주소를 메소드에 잇고 주소 표의 한 칸이 되는 자리·`value =` 표기와 생략 표기가 같은 뜻인 점, `@ResponseBody` 로 반환값을 응답 본문에 싣기와 붙이지 않으면 뷰 이름으로 읽히는 갈림·메소드 단위로 고를 수 있다는 점과 `@RestController` 를 쓸지의 기준, 반환 타입이 Content-Type을 정하는 규칙(`String` 만 `text/plain` 이고 기본형·`Map`·DTO는 `application/json`)과 문자열은 이미 글자라 더 바꿀 것이 없다는 정리, `Map<String, Object>` 를 그대로 돌려줘 키·값이 JSON이 되는 자리와 값 타입을 넓게 열어 섞어 담기·`HashMap` 이 순서를 보장하지 않아 응답 키 순서가 갈리는 점과 `LinkedHashMap`·DTO로 모양을 고정하는 대응, `@Data` 가 붙은 DTO를 setter로 채우고 getter가 JSON 키를 정하는 자리·컴파일 시점에 생성된 메소드를 실행 중에 읽어 하나의 응답을 만드는 두 갈래의 결합, `@RequestMapping` 으로 주소 앞부분을 클래스에 묶기와 `@GetMapping`·`@PostMapping` 이 그 합성 표기라는 점, `@RequestParam` 으로 요청 값 받기와 `required`·`defaultValue`·이름 짝짓기·DTO로 한꺼번에 받을 때 기본 생성자와 setter가 쓰이는 자리, `HttpMessageConverter` 와 Jackson이 반환값을 실제로 바꾸는 자리·`@JsonProperty`·`@JsonIgnore`·`@JsonInclude` 로 키와 내보낼 필드 고르기·`@RequestBody` 로 들어오는 JSON을 객체로 되돌리기, **나가는 쪽에서 들어오는 쪽으로** — `@Component`→`@Controller`→`@RestController` 로 겹쳐 올라가는 표시의 계단과 `@RestController` 를 붙이면 메소드마다 `@ResponseBody` 를 생략할 수 있는 자리·화면을 돌려주면 `@Controller`·값을 돌려주면 `@RestController` 로 갈리는 기준, `@RequestMapping` 을 클래스에 올려 공통 URL을 정하고 클래스 값과 메소드 값이 이어 붙는 규칙·주소 체계를 한 자리에서 바꿀 수 있는 값어치, `@RequestParam` 으로 쿼리스트링·폼 값을 매개변수에 받기와 이름이 짝을 맞추는 기준·HTTP로 오는 문자열을 매개변수 타입에 맞춰 스프링이 변환해 주는 자리와 `Integer.parseInt` 를 직접 적던 서블릿 방식과의 대비, `@RequestParam` 을 생략할 수 있는 조건과 매개변수 이름이 `.class` 에 남지 않을 수 있어 명시해 두는 편이 안전한 이유, `name` 으로 요청 쪽 이름 지목하기·`required = false` 로 선택 값 만들기와 기본형에 `null` 을 담을 수 없어 래퍼 타입이나 `defaultValue` 가 필요해지는 자리·`defaultValue` 가 숫자가 아니라 문자열인 이유가 애노테이션 속성 타입 제한과 이어지는 점, `@RequestParam Map<String, Object>` 로 파라미터를 통째로 받기와 내보낼 때의 `Map` 과 방향만 반대인 구조·값이 전부 문자열로 들어오고 무엇을 받는지가 서명에 드러나지 않는 한계·키가 정해지지 않은 자리에 어울리는 점, `@ModelAttribute` 로 값을 DTO에 담아 받기와 기본 생성자로 객체를 만들고 setter로 채우는 세 단계가 `newInstance`·`invoke` 와 같은 구조라는 정리·`@Data` 가 그 통로(기본 생성자·setter·`toString`)를 만들어 주는 자리, 값을 받는 세 방식을 받는 모양·드러남·타입 변환·확장으로 갈라 보고 값 개수에 따라 고르는 기준, `@PathVariable` 로 주소 자체에 값을 싣기와 `@RequestParam` 과의 갈림(자원을 가리키는 식별자는 주소에·걸러 내는 조건은 쿼리스트링에), `@ModelAttribute` 와 `@RequestBody` 가 값을 읽는 곳으로 갈리는 자리와 DTO가 비어 들어올 때 먼저 볼 지점, `@DeleteMapping` 으로 주소는 같고 방식으로 갈리는 매핑 만들기와 GET·POST·PUT·DELETE 짧은 표시가 모두 `@RequestMapping(method=…)` 의 합성이라는 점·DELETE에 본문 대신 쿼리스트링·주소로 값을 싣는 편이 무난한 이유, `@PathVariable` 로 값을 주소의 일부로 받기와 중괄호 자리 표시를 여러 개 두어 나눠 받는 자리·괄호 안 이름이 주소 쪽 자리를 지목하는 값이며 생략 조건이 `@RequestParam` 과 같다는 점·경로 값도 문자열로 도착해 매개변수 타입에 맞춰 변환되는 자리, `@RequestBody` 로 요청 본문의 JSON을 DTO로 되돌리기와 `@ModelAttribute` 와 코드 모양이 같은데 값이 실려 오는 곳으로 갈리는 자리·본문을 실을 수 있는 `@PostMapping`·`@PutMapping` 과 짝을 이루는 이유·주소에 값이 드러나지 않는 점·나가는 쪽 직렬화의 반대 방향이 기본 생성자와 setter로 도는 자리, 값을 받는 네 통로(`@RequestParam`·`@ModelAttribute`·`@PathVariable`·`@RequestBody`)를 "어디에 실려 오는가 × 무엇으로 받는가" 두 축으로 정리하기와 앞 축은 보내는 쪽이·뒤 축은 값 개수가 정한다는 점·DTO가 비어 들어올 때 요청 `Content-Type` 과 받는 표시가 짝을 이루는지부터 보는 자리, `HandlerMethodArgumentResolver` 로 매개변수에 값이 채워지는 실제 자리와 표시별 담당자·`invoke(대상, 값1, 값2)` 로 미리 만든 값을 한 번에 넘기는 구조·값 변환 실패가 메소드 본문 전에 걸러지는 이유 | [[Spring day03 애노테이션과 리플렉션]] |
| Spring day04 | **REST 컨트롤러 CRUD 골격 — 표시들을 한 컨트롤러에 조립하기** — 연습 패키지에 진입점을 두고 컴포넌트 스캔 범위를 실습 단위로 나누기와 진입점이 여럿일 때 살아나는 컨트롤러가 갈리는 자리, `@RestController` 로 빈 등록·요청 수신·본문 응답을 한 번에 얻기와 `@Controller`+`@ResponseBody` 합성이라는 점·화면을 돌려줄 일이 없어 이쪽을 고르는 기준, `@RequestMapping` 을 클래스에 올려 공통 주소 앞머리를 정하고 클래스 값과 메소드 값이 이어 붙는 규칙·괄호를 비운 매핑이 클래스 주소를 그대로 쓰는 자리·주소 체계를 한 자리에서 바꾸는 값어치와 컨트롤러가 여럿일 때 충돌을 줄이는 배치, `@PostMapping`·`@GetMapping`·`@PutMapping`·`@DeleteMapping` 넷을 한 클래스에 모아 CRUD 골격 만들기와 주소가 같아도 방식이 달라 충돌하지 않는 이유(주소+방식이 한 자리를 만든다)·넷이 모두 `@RequestMapping(method=…)` 의 합성이라는 점·서블릿의 `doGet`·`doPost`·`doPut`·`doDelete` 와 갈림의 기준은 그대로이고 표시만 바뀐 자리·자원 중심 주소 설계의 기본형(무엇을은 주소가·어떻게는 방식이 말한다), 목록과 상세가 둘 다 GET일 때 주소로 갈라 두기와 쿼리스트링·경로 변수 중 고르는 기준(걸러 내는 조건은 쿼리스트링에·자원을 가리키는 식별자는 주소에)·주소 설계는 옳고 그름보다 한 프로젝트 안의 일관성이 먼저라는 점, DAO를 지역변수가 아니라 멤버변수로 들고 싱글톤 창구(`getInstance()`)로 꺼내 오기와 컨트롤러 자신도 하나로 공유되는 빈이라 객체가 늘지 않는 구조·구현 클래스 이름이 컨트롤러에 박혀 교체가 막히는 결합도 문제가 그대로 남는 자리, 컨트롤러 본문이 넘기고 받고 돌려주는 세 줄로 다섯 메소드가 같은 모양인 점과 Controller·Dao·Dto가 맡는 일의 갈림·바뀌는 이유가 다른 것을 섞지 않으려고 컨트롤러를 얇게 두는 이유·메소드 이름을 층 사이에 관통시켜 따라 읽기 쉽게 두기, 반환 타입이 응답 모양을 정하는 규칙의 재확인(`boolean` 은 성공 여부·`ArrayList<DTO>` 는 JSON 배열·DTO는 JSON 객체)과 DTO 필드 이름이 getter를 거쳐 JSON 키가 되고 화면에서 꺼내는 이름이 되는 관통·`executeUpdate` 의 건수를 `boolean` 으로 줄여 둔 모양·`boolean` 하나로는 0건과 오류를 갈라 보이지 못하는 한계, 값을 받는 두 모양이 한 클래스에 같이 있는 자리(DTO로 통째로 받기와 값 하나만 받기)와 각각 `@ModelAttribute`·`@RequestParam` 이 생략된 것이라는 점·매개변수 이름이 `.class` 에 남지 않을 수 있어 이름을 적어 두는 편이 안전한 이유·DTO에 값이 채워지는 과정이 기본 생성자와 setter로 도는 자리, `ResponseEntity`·`HttpStatus` 로 상태 코드와 헤더까지 돌려주기와 상황별 코드(201·200·404·400·500)·200에 `false` 를 실으면 화면 쪽 판정이 하나 더 늘어나는 자리, 조회 결과가 없을 때 `null` 대신 빈 리스트를 돌려주는 편이 다루기 쉬운 점(없음을 값의 부재가 아니라 빈 값으로 표현하기)과 상세 조회는 404로 갈라 주는 갈림, 같은 주소·같은 방식이 겹치면 요청 때가 아니라 서버가 뜰 때 걸리는 이유가 주소 표를 미리 만드는 구조와 이어지는 자리, 매핑이 안 먹힐 때 스캔 범위→클래스 표시→이어 붙은 실제 주소→방식 순으로 좁혀 보기와 서버가 뜰 때 찍히는 매핑 목록을 확인하기·값이 비어 들어올 때는 보내는 쪽 `Content-Type` 과 받는 쪽 표시의 짝부터 보기, `@Repository` 로 DAO를 빈으로 등록하고 `final` + `@RequiredArgsConstructor` 로 생성자 주입 받기와 직접 만들 때와의 갈림(만드는 주체·박히는 이름·테스트에서 다른 구현을 넣을 수 있는 값어치)·필드 `@Autowired` 보다 생성자 쪽이 나은 이유, 계층별 표시(`@Controller`·`@Service`·`@Repository`)가 이름만 갈린 `@Component` 라는 점과 `@Repository` 의 DB 예외 변환, 매개변수를 여러 개 늘어놓는 대신 DTO로 묶어 값에 이름을 남기기와 위치로만 구분되는 값이 늘 때의 위험, 도메인이 늘어도 다섯 줄 골격은 그대로고 이름만 갈리는 되풀이와 그것을 줄이는 두 방향(공통을 상속·제네릭으로 올리기 vs `JdbcTemplate`·JPA로 코드를 안 쓰기), Controller와 Dao 사이에 Service 계층이 놓이는 자리와 여러 단계를 한 묶음으로 묶는 `@Transactional`, `@Valid`·`@NotBlank`·`@Min` 으로 값 검사를 메소드 본문 앞으로 당기기와 DB 제약보다 앞에서 막는 값어치, `@RestControllerAdvice`·`@ExceptionHandler` 로 예외 처리를 한 자리에 모으고 응답 모양을 통일하기, **오가는 값을 담는 DTO를 롬복 표시 넷으로 만들기** — `@NoArgsConstructor`·`@AllArgsConstructor` 를 짝으로 붙이는 이유(자바는 생성자를 하나도 안 적었을 때만 기본 생성자를 넣어 주므로 전체 생성자를 만들면 기본 생성자가 사라지고, 스프링의 값 바인딩은 빈 객체를 먼저 만든 뒤 setter로 채우는 순서라 첫 단계에서 막힌다)·`@Setter` 가 들어오는 길을 `@Getter` 가 나가는 길을 여는 자리와 하나만 빠져도 값이 비어 들어오거나 빈 JSON이 나가는 점·`@AllArgsConstructor` 로 목록 더미를 한 줄씩 채워 DB를 붙이기 전에 응답 모양부터 화면과 맞춰 보기·`@Data` 묶음이 함께 데려오는 것들과 표시를 하나씩 고를 때 이유가 코드에 남는 점·`@Setter` 를 빼는 갈래는 받는 통로가 생성자여야 하고 폼 바인딩은 기본 생성자·setter를 쓴다는 갈림·요청용 DTO와 응답용 DTO를 갈라 두는 값어치와 내보내면 안 되는 값이 응답에 섞이는 사고·`@JsonIgnore` 로 응답에서 필드 빼기, `int` 와 `Integer` 의 갈림 — 차이는 `null` 하나뿐이지만 HTTP를 사이에 두면 값이 아예 안 오는 경우가 늘 있어 기본형에는 "없음"을 담을 자리가 없고 0으로 채워지면 "안 보냈다"와 "0을 보냈다"가 같은 모양이 되는 문제·경계를 넘어오는 값은 래퍼 타입·계산용 지역변수는 기본형이라는 기준·언박싱 시점의 예외와 `==` 대신 `equals` 로 값 비교하기, 값이 어디에 실려 오는지 표시로 적어 두기 — `@RequestBody`(본문 JSON)·`@RequestParam`(쿼리스트링)·`@PathVariable`(주소 경로) 셋을 한 클래스에 나란히 두기와 `@RequestBody`·`@ModelAttribute` 가 모양은 같고 읽는 곳으로 갈리는 자리·보내는 쪽 `Content-Type` 이 받는 쪽 표시를 정한다는 재확인·표시를 생략할 수 있는 것과 생략하는 편이 나은 것은 다르다는 점(매개변수 이름이 `.class` 에 안 남을 수 있어 이름에 기대는 매칭은 환경에 따라 어긋난다)·`@DeleteMapping("/{no}")` 의 자리 표시와 `@PathVariable(name=…)` 의 이름을 맞춰야 값이 채워지는 자리, **연결 정보를 코드 밖 설정 파일로 옮기기** — `application.properties` 의 `키=값` 한 줄과 `#` 주석 표기·값에 따옴표를 두르지 않는 규칙, `server.port` 로 내장 톰캣 포트를 파일에 드러내 두기와 기본값과 같아도 적어 두는 값어치, `spring.datasource.url`·`username`·`password` 세 줄로 DB 연동 정보 적기와 DAO 안에 문자열로 박아 두던 방식과의 갈림(값을 바꿀 때·환경별로 다를 때·값을 찾을 때)·바뀌는 이유가 다른 것을 갈라 두는 원칙, `src/main/resources` 가 빌드하면 클래스패스 루트가 되어 스프링이 경로 없이 찾아 읽는 자리, `@Value` 로 값 하나를 꺼내기와 `@ConfigurationProperties` 로 앞머리가 같은 묶음을 객체로 받기의 갈림·스프링이 이미 쓰는 값은 직접 읽을 일이 적은 이유, 비밀번호를 파일에 그대로 두지 않는 갈래(`${}` 치환·환경변수·실행 옵션·`.gitignore` 로 뺀 파일·프로파일 분리)와 설정 우선순위(실행 옵션 > 환경변수 > 파일)·한 번 올라간 값은 이력에 남는다는 점, **표시를 처리해 줄 도구를 의존성으로 선언해 두기** — 롬복 표시를 붙여도 애노테이션 프로세서가 빌드에 끼어 있어야 코드가 생긴다는 점과 `compileOnly`(표시를 알아보는 쪽)·`annotationProcessor`(표시를 읽어 코드를 만드는 쪽)가 짝인 이유·하나가 빠지면 컴파일이 막히거나 메소드가 안 생기는 자리, 의존성 앞말이 그 라이브러리가 필요한 구간을 정하는 규칙(`implementation`·`compileOnly`·`runtimeOnly`·`annotationProcessor`·`testImplementation` 의 컴파일·실행·결과물 포함 여부)·롬복이 결과물에 남지 않아도 되는 이유(컴파일이 끝나면 `.class` 에 평범한 getter·setter가 들어 있다)와 MySQL 드라이버가 `runtimeOnly` 인 반대 사정(JDBC 인터페이스에만 기대고 구현은 실행 시점에 찾는다)·필요한 구간만큼만 열어 두는 값어치, 스타터가 라이브러리 하나가 아니라 같이 쓰는 것들의 꾸러미라는 점과 JPA로 넘어가기 전 얹어 두는 자리, 진입점이 늘수록 `springBoot { mainClass }` 한 줄로 띄울 실습을 고르는 구조와 컴포넌트 스캔 범위 이야기의 맞물림, **설정 몇 줄이 객체가 되는 자리** — 값을 읽어 쓰는 코드가 없는데도 커넥션이 만들어지는 자동 설정과 `@EnableAutoConfiguration` 의 조건 판정(드라이버가 있나·설정이 있나·빈이 이미 있나)·조건이 `@ConditionalOnClass` 같은 표시로 적혀 있어 프레임워크 자신에게도 같은 구조가 쓰이는 점·의존성을 넣는 일이 곧 기능을 켜는 일이 되는 자리와 `--debug` 로 판정 결과 목록 보기·대신 무슨 일이 일어나는지가 코드에 안 보이는 뒷면 | [[Spring day04 REST 컨트롤러 CRUD 골격]] |
| Spring day04 | **JPA 엔티티와 리포지토리 — SQL을 적지 않고 표를 다루기** — 되풀이를 줄이는 두 방향 중 "코드를 안 쓰고 규약으로 대신하는" 갈래를 실제로 밟아 보는 자리, `sample.sql` 로 옮겨 담을 표를 먼저 만들어 두기와 `PRIMARY KEY AUTO_INCREMENT` 가 엔티티 표시와 짝을 이루는 점·`spring.datasource.url` 이 가리키는 DB 이름과 맞춰야 붙는 자리, `@Entity` 로 클래스를 표와 짝지어 관리한다고 선언하기와 `@Table(name=…)` 으로 표 이름 지목하기·생략하면 클래스 이름이 표 이름이 되는 규칙, 필드와 컬럼이 이름으로 짝을 이루어 컬럼→필드→JSON 키→화면으로 이름 하나가 관통하는 자리, DTO와 엔티티가 서 있는 자리의 갈림(오가는 값의 모양 vs DB의 표·그냥 객체 vs 상태가 추적되는 객체), `@Id` 로 열쇠를 지정하고 엔티티가 PK를 반드시 하나 이상 갖는 이유(객체가 표의 어느 줄인지 늘 알아야 한다)와 DB의 `PRIMARY KEY` 와 양쪽에서 같은 이야기를 하는 구조, `@GeneratedValue` 로 번호 만들기를 맡기기와 `GenerationType` 네 갈래(`IDENTITY`·`SEQUENCE`·`TABLE`·`AUTO`)·MySQL의 `AUTO_INCREMENT` 와 `IDENTITY` 가 짝인 이유·저장 전 `null` 과 저장 후 채워진 번호를 갈라 보려면 `Integer` 여야 하는 자리, 엔티티에 붙은 롬복 표시 넷과 JPA 규격이 매개변수 없는 생성자를 요구하는 이유(빈 객체를 먼저 만든 뒤 값을 채운다)·`@Builder`·`@AllArgsConstructor` 와 기본 생성자를 함께 두는 관용·`@Data` 를 엔티티에 붙일 때 딸려 오는 `@EqualsAndHashCode`(값이 바뀌면 다른 객체가 된다)·`@ToString`(연관이 얽히면 순환)·`@Setter`(변경 시점이 흩어진다)가 걸리는 지점과 PK만 보게 두거나 통로를 메소드로 만드는 갈래, **`extends JpaRepository<엔티티, PK타입>` 한 줄로 구현 없이 메소드를 물려받기** — 몸통이 빈 인터페이스가 도는 이유(스프링이 시작할 때 찾아 구현 객체를 만들어 빈으로 등록한다)와 "표시를 읽어 동작을 만드는" 구조가 인터페이스 자체를 읽는 쪽으로 간 정리·제네릭 두 자리가 조작할 엔티티와 PK 타입을 적는 곳이며 `@Id` 필드 타입과 맞아야 하는 점·물려받는 메소드들(`findAll`·`findById`·`save`·`deleteById`·`count`·`existsById`)과 대응하는 SQL·CRUD 네 갈래가 이름만 바뀌어 그대로 있는 점·`@Repository` 없이도 등록되지만 붙이면 계층이 드러나는 값어치·직접 JDBC와 견줬을 때 연결·SQL 문자열·값 바인딩·결과 훑기·자원 닫기가 전부 사라지는 자리와 무슨 SQL이 나가는지 코드에 안 보이는 뒷면·메소드 이름을 단어로 쪼개 읽어 쿼리를 만드는 규칙(`findByEname`·`Containing`·`GreaterThan`·`OrderBy`)과 필드 이름이 어긋나면 뜰 때 걸리는 점·조건이 늘면 `@Query` 로 넘어가는 기준, **Controller와 Repository 사이에 Service 계층 두기** — `@Service` 가 `@Component` 와 결과는 같고 이름이 계층을 말해 주는 점·넘기기만 하는 자리와 리포지토리가 돌려준 값을 보고 판정하는 자리의 갈림·양쪽 어디에 둬도 어색한 코드를 담는 것이 Service의 값어치, `final` + `@RequiredArgsConstructor` 로 만들지 않고 받기와 컴파일 시점 코드 생성(애노테이션 프로세서)과 실행 시점 주입(IOC/DI)이 맞물려 한 줄로 보이는 자리·`getInstance()` 로 꺼내 오며 남겨 뒀던 결합도 문제가 풀리는 지점·각 계층이 자기 바로 아래만 아는 배치, `save` 가 영속된 엔티티를 돌려주므로 DB가 채운 번호를 그 자리에서 확인할 수 있는 점과 `insert`·`update` 가 PK가 비어 있는가로 갈리는 기준(`int` 였다면 0이 "3번을 고쳐라"로 읽힌다), 엔티티를 그대로 주고받는 컨트롤러와 표의 모양이 곧 응답의 모양이 되는 문제(컬럼이 늘면 키가 늘고·내보내면 안 되는 값이 같이 나가고·요청에 PK가 실려 오면 등록이 수정으로 갈린다)와 요청용·응답용을 갈라 두는 방향·`@JsonIgnore`·`boolean` 대신 저장된 엔티티와 상태 코드를 돌려주기·`Page`·`Pageable`·`Sort` 로 쪽 나누기, `spring-boot-starter-data-jpa` 한 줄이 데려오는 것들(JPA 규격·하이버네이트 구현체·Spring Data JPA·커넥션 풀과 트랜잭션)과 의존성을 넣는 일이 곧 기능을 켜는 일이 되는 자동 설정 구조의 재확인·맨 아래층은 여전히 JDBC라 드라이버가 `runtimeOnly` 로 남는 점, `spring.jpa.show-sql`·`format_sql` 로 나가는 SQL을 로그로 옮겨 보기와 `spring.jpa.hibernate.ddl-auto` 다섯 값의 갈림(`create` 계열은 뜰 때 표를 지운다·표는 SQL로 만들고 `validate` 로 두는 배치), `@Transactional` 이 Service에 붙는 이유와 `readOnly`·프록시 구조와 자기 호출 제약, `@Column`·하이버네이트 네이밍 전략(카멜↔스네이크)과 자바빈 프로퍼티 규약이 갈리는 자리, **영속성 컨텍스트** — 읽어 온 엔티티를 담아 상태를 추적하는 상자·1차 캐시로 같은 열쇠는 같은 객체가 나오는 점·더티 체킹으로 `save` 없이 `update` 가 나가는 자리, JPA(규격)·하이버네이트(구현체)·Spring Data JPA(편의 층) 세 층의 갈림과 JDBC·드라이버 관계와 같은 모양, 연관관계 매핑 넷과 객체를 따라가는 모양의 값어치·한 줄이 몇 번의 쿼리가 되는지 안 보이는 N+1 문제와 `fetch join`·`@EntityGraph`, JPQL이 표가 아니라 클래스를 대상으로 적는 질의라는 점과 Querydsl로 질의를 자바 코드로 조립해 컴파일 시점에 검사받기 | [[Spring day04 JPA 엔티티와 리포지토리]] |
| DB | SQL DDL·DML·스키마 설계 | [[SQL day01 데이터베이스 기초]] · [[SQL day02 테이블과 제약조건]] · [[SQL day03 DML과 조인]] · [[SQL day04 집계와 정렬]] · [[SQL day05 외래키 CASCADE와 조인]] |

### 2026_FE (프론트 15일)

| day | 주제 | 노트 |
| --- | --- | --- |
| day02 | HTML 기본 마크업 + JS 첫 실행 | [[HTML day02 문서 구조와 미디어]] · [[JS day02 변수와 입출력]] |
| day03 | JS 자료형 | [[JS day03 자료형과 연산자]] |
| day04 | JS 조건문 / HTML 폼·테이블 | [[JS day04 조건문]] · [[HTML day04 폼과 테이블]] |
| day05 | JS 반복문 / CSS 첫 실습 | [[JS day05 반복문]] · [[CSS day05 첫 스타일링]] |
| day06 | CSS 선택자·텍스트·박스 | [[CSS day06 선택자와 기본 속성]] |
| day07 | JS 객체 | [[JS day07 객체]] |
| day08 | CSS flex | [[CSS day08 flexbox]] |
| day09 | 카페 키오스크 화면 | [[CSS day09 카페 키오스크]] |
| day10 | JS 함수 / 강의 사이트 레이아웃 | [[JS day10 함수]] · [[CSS day10 카메라 강의 사이트]] |
| day11 | JS DOM / 커뮤니티·예약 사이트 | [[JS day11 DOM 조작]] · [[CSS day11 커뮤니티와 예약 사이트]] |
| day12 | 제품·사원 관리 CRUD (데이터 모델링) | [[JS day12 제품 사원 관리 CRUD]] |
| day13 | 웹 스토리지·쿼리스트링·인터벌 | [[JS day13 웹 스토리지와 인터벌]] |
| day14 | 게시판 CRUD / CSS position | [[JS day14 게시판 CRUD]] · [[CSS day14 position과 가상요소]] |
| day15 | CSS 테이블·이미지·배경 | [[CSS day15 테이블과 배경]] · [[HTML day15 테이블 마크업]] |
| 과제 | LevelUP, Message_Board | [[JS 과제 LevelUP과 게시판]] |

## 프론트와 백엔드가 만나는 지점

같은 문제를 두 언어로 푼 대목입니다. 짝지어 보면 개념이 단단해집니다.

| 문제 | 프론트엔드 | 백엔드 |
| --- | --- | --- |
| 데이터 영속화 | `localStorage` + JSON | MySQL 테이블 |
| 빈 자리 찾기 | `index === -1` | `posts[i] == null` |
| 자동 번호 | `마지막.no + 1` | `AUTO_INCREMENT` |
| 데이터 묶기 | 배열 + `find` | `JOIN` |
| 데이터 구조 | 객체 `{ }` | DTO 클래스 |
| 로직 분리 | `.js` 파일 분리 | Controller / Repository |
| 값 검증 | `if (!title) return` | setter 안 유효성 검사 |

가장 중요한 연결: [[JS day14 게시판 CRUD]] · [[Java day06 생성자와 콘솔 게시판]] · [[Java day09 MVC 종합예제]] 는 **같은 게시판을 다른 층·다른 구조로 만든 것**입니다. 셋을 나란히 놓고 보면 CRUD의 본질과 계층 분리의 이유가 동시에 보입니다.

## 프로젝트 매핑표 — 분석 프로젝트와 학습 노트의 연결

실제 프로젝트 분석은 [[프로젝트 코드 분석 안내]] 아래에 있고, **개별 분석 노트와 day 노트는 서로 직접 링크하지 않습니다.** 프로젝트에서 만난 주제가 어느 학습 노트와 닿는지는 이 표에서 찾습니다. (다리는 프로젝트 MOC ↔ 이 학습 지도 하나뿐)

### [[보드게임카페예약사이트 프로젝트 MOC]] ↔ 학습 노트

| 프로젝트 구간 | 다룬 주제 | 해당 학습 허브 |
| --- | --- | --- |
| `back-end/routes` | 예약 겹침 판정, 화이트리스트 정렬, 트랜잭션 부재, N+1 | [[CS 이론 MOC]] (SQL day01·03·04) |
| `back-end/controllers·models` | MVC 계층, Repository 미완, 싱글톤 | [[Java MOC]] (day09 MVC) |
| `back-end` 인증 | 평문 비밀번호, localStorage 세션 | [[CS 이론 MOC]] · [[Java MOC]] (캡슐화·접근제한) |
| `crawler/` | Selenium 수집, 멱등 업서트, ETL | [[Python 정리본 인덱스]] · [[CS 이론 MOC]] (제약조건) |
| `front-end/hooks` | 커스텀 훅·클로저, 낙관적 업데이트, 무한 스크롤 | [[JavaScript MOC]] (day07 객체·day10 함수·day13 스토리지) |
| `front-end/components` | 게시판형 CRUD 화면, 폼 처리 | [[JavaScript MOC]] (day12·day14) |
| styled-components 레이아웃 | position 오버레이, flexbox 그리드, 폼 마크업 | [[CSS MOC]] · [[HTML MOC]] |
| `학습 관점/` 시리즈 | 위 주제들의 공부 순서·연습 과제 | (프로젝트 MOC에서 진입) |

## 폴더 규칙 (관찰된 패턴)

| 폴더 | 용도 |
| --- | --- |
| `exam/` | 개념 정리 코드. 주석이 가장 많고 학습 가치가 높음 |
| `practice/` | 스스로 푼 연습문제 |
| `activity/` | 실습 과제 |
| `assets/` | 이미지·미디어 |

## 다음 단계 로드맵

**단기** — [[JS day14 게시판 CRUD]] 를 `common/storage.js`로 리팩터링 / [[Java day06 생성자와 콘솔 게시판]] 을 ArrayList 버전으로 재작성

**중기** — JDBC로 [[Java day07 메소드와 미니프로젝트]] 를 MySQL에 연결 / FE 게시판을 `fetch` + 백엔드 API로 전환

**장기** — Spring Boot + JPA / React / 배포 (시작 지점: [[Spring Boot 프로젝트 생성(분석)]])

## 관련

- [[옵시디언-노션-VSCode 운영 규칙]]
- [[프로젝트 코드 분석 안내]]
