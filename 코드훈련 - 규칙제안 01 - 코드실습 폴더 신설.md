---
출처: Claude 분석
작성일: 2026-08-21
tags: [규칙]
---

# 코드훈련 - 규칙제안 01 - 코드실습 폴더 신설

> 작성 세션: 코드훈련 세션
> 장부: [[코드훈련 - 규칙제안 기록]]

## 발견

코드훈련 세션(`코드정리/` 레퍼런스로 실습 미션을 주고 사용자가 직접 코드를 짜게 하는 세션)에서, 사용자가 직접 짠 코드 원문과 개선점 정리를 Vault에 남기고 싶다고 요청했다. 현재 세션의 산출물 규칙은 "Vault에 쓰지 않음"(코드는 사용자 컴퓨터의 별도 폴더에만 저장)이라 이 요청을 처리할 근거 폴더가 없다.

또한 `코드정리/`는 자기 루트 노트에 "다른 폴더(학습노트·MOC·프로젝트)와도 연결하지 않는 고립 구역"이라고 명시되어 있어, 새 폴더와의 연결도 규칙 위반이 된다.

## 제안

1. **최상위 폴더 `코드실습/` 신설.** 최상위 폴더 생성은 CLAUDE.md상 마스터 세션 전담이라 직접 만들지 않고 여기서 요청한다
2. **관할 세션 지정.** `코드실습/`은 코드훈련 세션이 쓰기 관할을 갖는다 (세션 역할 분담표에 한 줄 추가 제안)
3. **`코드정리/` ↔ `코드실습/` 연결 방식.** `코드정리/`의 "다른 폴더와 연결 안 함" 원칙에 이 폴더 하나만 예외를 둔다
   - `코드실습/`의 각 노트는 사용한 `코드정리/` 노트로 링크를 건다 (실습 → 레퍼런스, 단방향)
   - `코드정리/` 쪽에서 `코드실습/`으로 가는 역방향 링크는 걸지 않는다 (코드정리는 여러 실습에서 재사용되므로 역링크를 걸면 노트마다 실습 목록이 계속 늘어나 트리가 아니라 그물이 된다)
4. **노트 형식(초안).** 미션 1건 = 노트 1개. `## 코드 원문` (있는 그대로, 오류 안 고침) + `## 고치면 좋은 점` (근거는 사용한 코드정리 노트명과 함께). frontmatter는 기존 관례(`출처`·`작성일`·`tags`)를 따르고 태그는 유형 `학습` + 영역 태그
5. **파일명(초안).** `코드실습 - <미션명>.md`

## 판단이 필요한 지점

- `코드정리/` 고립 규칙에 예외를 하나 두는 것이므로, 코드정리 루트 노트(`코드정리.md`)의 문구도 함께 손볼지 마스터 세션 판단이 필요하다
- 노트 형식·파일명은 초안이라 마스터 세션이 기존 템플릿 체계(`_템플릿/`)와 맞춰 확정해도 된다
- 미션이 쌓였을 때 `코드실습/` 내부를 트리로 관리할지(수집기 패턴) 평문 나열로 둘지는 아직 실습이 몇 건 안 쌓여 판단 이르다 — 나중에 재제안 가능

## 결정 기록

**채택** (2026-08-26, 마스터 세션)

- 제안 1~5 전부 채택: `코드실습/` 신설(안내 노트·실습 목록 표 포함), 관할=코드훈련 세션(역할표에 코드정리·코드실습 병기), 코드정리 고립 규칙에 단방향 예외 명문화(코드정리.md 문구 수정), 노트 형식·파일명 초안 그대로 확정
- 판단 지점: 별도 템플릿은 만들지 않음(형식이 단순). 내부 트리화는 보류 — 실습이 쌓이면 재제안
- 부수 조치: sync-config categoryMap에 `코드실습`과 함께 누락돼 있던 `코드정리`·`프로젝트 노트`도 등록 (노션 "운영" 오분류 해소)
