# 옵시디언 Vault 작업 지침

이 폴더는 KDT 학습노트 + 프로젝트 분석이 모인 옵시디언 Vault다. Cowork(Claude 데스크톱)와 Claude Code가 같은 규칙으로 작업하기 위한 공유 지침이며, 상세 규칙 원문은 아래 파일을 따른다.

@_규칙/옵시디언-노션-VSCode 운영 규칙.md

## 핵심 규칙 요약

### 파이프라인과 역할

```
VS Code (읽기 전용) → Claude (분석·작성) → Obsidian Vault (원본)
                                              ├→ Node.js Sync Engine → Notion (거울)
                                              └→ obsidian-git → GitHub (백업)
```

- **옵시디언이 원본(Single Source of Truth).** 노션 미러링에는 개입하지 않는다 (Sync Engine 담당)
- VS Code의 학습 코드(`KDT_2026`)는 **읽기 전용** — 절대 수정하지 않는다
- 원본 코드의 오류·오타는 노트에 기록하지 않는다

### 원본 보존 (사용자가 직접 쓴 노트)

- 가능: 디자인·형식 정리, 오탈자 수정, 추가 설명 덧붙이기, 문맥 정리
- 불가: 내용 삭제, 의미 변경 — 필요하면 먼저 보고하고 승인받기
- 사실과 다른 내용도 지우지 말고 아래에 보충

### 문체

- 학습노트(언어 폴더): **한 사람이 공부하며 직접 정리한 것처럼.** 2인칭(하신·쓰셨) 금지, 리뷰어 어조(잘했습니다·정확합니다) 금지
- `프로젝트 코드 분석/`: **개발자 일지** — 목표 → 살펴본 것 → 판단·근거 → 막힌 것. 분석 + 피드백 구조, 추측도 1인칭으로
- 학습노트 3단 구성: 배운 내용 → 추가 활용법 → 더 나아가기

### 노트 형식

- 모든 새 노트에 frontmatter: `출처`(Claude 분석/내가 작성/이관), `원본`, `작성일`, `tags`
- **태그는 `_규칙/태그 사전.md` 등록 어휘만** (유형 1개 — `허브`/`학습`/`프로젝트`/`용어`/`규칙`/`개념`/`코드정리` + 영역 0~2개). 인라인 태그도 사전 등록제. 미등록 태그는 위반(Warning). day 번호·주제어 태그 금지 — 찾기는 지도·파일명(Ctrl+O)·전문 검색(Ctrl+Shift+F)이 담당
  - `개념` 유형 = 개념 노트(가로 관문) 전용. 예: `멱등성.md` → `tags: [개념]` — 태그 패널에서 개념 노트만 모아볼 수 있다
- 폴더 제목에 이모지 금지
- 파일명: `<언어> day<NN> <주제>.md` / MOC는 `<주제> MOC.md`
- **명칭 통일**: 같은 대상의 표기는 하나만 쓴다 — 폴더는 정식명(`JavaScript`), 파일 접두사는 구역 규칙표의 prefix(`JS `), 그 외 표기(자바스크립트 등) 혼용 금지. 제목·태그·본문 용어 모두 동일 원칙
- **한 노트 = 핵심 주제 1개**: 핵심 내용이 2개 이상 담기면 노트를 분리한다 (과도한 장문 방지). 영역 태그가 2개를 넘게 필요해지는 것이 분리 신호
- 위키링크 필수: 상하 링크(허브·이전·다음), 문서 끝 `## 관련 노트`, MOC에 소속 노트 전부 링크
- **언어 폴더도 트리 규칙** (2026-09 대정리): day 노트의 링크는 허브(MOC)·이전·다음·개념 노드·학습 지도만. 언어 간 교차 링크 금지 — 걸고 싶으면 전역 개념 승격 신호이거나 평문 언급으로. 정규 소속(트리 부모)은 노트당 1개, 탐색 경로는 지도·개념·태그·검색 여러 채널로 열려 있다
- **전역 개념 층 `개념/`**: 여러 구역에 걸친 개념의 관문 (구역 고립의 유일한 전역 예외). 승격은 "서로 다른 구역 2곳 이상 등장"(기계 판정) 후 **사용자 승인**(구역 비종속·의미 동일·실제 도움 확인). 개념 노드는 등장 위치로만 링크하고 **개념끼리는 잇지 않는다** (자유 연결 금지 — 관계 타입 도입은 필요가 반복 확인되면 재논의)
- **구역별 상세 규칙은 `_규칙/구역 규칙.yaml`이 원본** — 검사기·Sync·RAG도 이 파일을 읽는다. 파일명은 구역 규칙표의 접두사를 따른다
- **프로젝트 분석 ↔ KDT 학습 노트 직접 링크 금지** — 프로젝트마다 `<프로젝트명> MOC`를 상위 노드로 두고, 학습과의 연결은 `프로젝트 MOC ↔ KDT_2026 학습 지도`(매핑표) 경유. 프로젝트끼리도 링크하지 않는다

### Git

- 작업을 마칠 때마다 의미 있는 메시지로 커밋 (`SQL day04 노트 추가` 형식, 한국어)
- 푸시는 obsidian-git 자동 동기화(30분)가 처리하므로 커밋만 남겨도 된다
- 원격: `github.com/SeungMinShin01/obsidian-til` (Public — 시크릿·토큰·개인정보 커밋 금지)
- `.gitignore` 대상: `workspace.json`, `.smart-env/`, `.trash/`, `_규칙/sync-config.json`, `Fast-EQA/`
- **git 잠금·잔해 처리** (2026-09-01 재개정):
  - `HEAD.lock` 등 잠금을 만나면 **절대 바로 rename하지 않는다** — obsidian-git 자동 커밋(매 30분)이 잠깐 만드는 정상 파일이다. 30초~1분 대기 후 재시도. rename은 **10분 이상 묵은 잠금**에만 최후수단으로 (즉시 rename이 잔해 122건의 원인이었음)
  - 잔해(`tmp_obj_*`, `__trash_*`, `*.stale`류)는 rename 금지. **삭제가 되는 세션**은 즉시 삭제(`find .git -name "*.stale*" -delete`), **삭제가 `Operation not permitted`로 막히는 세션**은 잔해를 건드리지 말고 그대로 두고 마스터 세션에 보고만 한다 (마스터가 주기 청소). 잔해는 0바이트 쓰레기라 방치해도 동작엔 무해하다

### 폴더 구조

```
Java/ Spring/ Python/ JavaScript/ HTML/ CSS/ RPA/ AI/ CS 이론/   ← 학습노트 (day별)
프로젝트 코드 분석/<프로젝트명>/                            ← 실제 프로젝트 분석 (코드 폴더별 분산)
프로젝트 노트/                                            ← 사용자가 말한 내용 정리 (고립 구역)
Fast-EQA/                                                ← 비공개 고립 구역 (로컬 전용 — GitHub·노션 미동기화)
개념/                                                    ← 전역 개념 층 (여러 구역에 걸친 개념의 관문)
개인정리/                                                ← 면접 대비 — 사용자 초안 + AI 다듬기(의미 보존)
기술정리/                                                ← 기술블로그 정리본 (고립 구역, 공개 — 아래 참고)
코드정리/                                                ← 코드·문법·패턴 레퍼런스 트리 (고립 구역)
코드실습/                                                ← 코드훈련 미션 결과 (원문+개선점, 코드정리로 단방향 링크)
_규칙/  _템플릿/                                          ← 규칙과 템플릿
```

- `프로젝트 코드 분석/`은 부트캠프 학습 코드가 아니라 **실제 프로젝트 전용**
- 템플릿: `_템플릿/학습노트 템플릿.md` · `_템플릿/프로젝트 분석 템플릿.md`

### 프로젝트 노트/ — 고립 규칙 (중요)

`프로젝트 노트/`는 사용자가 말한 내용만 정리해 쌓는 **독립 구역**이다.

- 위키링크는 **이 폴더 안의 노트끼리만** 건다
- Vault의 다른 노트(학습노트, 프로젝트 코드 분석, MOC, 학습 지도, 전문용어 정리 등)로 **링크를 걸지 않는다**
- 반대 방향도 금지 — 다른 폴더의 노트에서 `프로젝트 노트/`로 링크를 걸지 않는다
- MOC·학습 지도에 이 폴더의 노트를 등록하지 않는다
- 그래프뷰에서 이 폴더는 별도의 섬으로 떠 있는 것이 정상이다

### Fast-EQA/ — 비공개 고립 구역

`Fast-EQA/`는 **로컬 옵시디언에서만 존재하는** 완전 고립 구역이다.

- `.gitignore` + `sync-config.json` exclude 대상 — **GitHub에도, 노션에도 올라가지 않는다**
- 위키링크는 **이 폴더 안의 노트끼리만** 건다. 바깥으로 나가는 링크도, 바깥에서 들어오는 링크도 금지 (링크 텍스트가 공개 노트 본문에 남기 때문)
- MOC·학습 지도·`Vault 홈`에 등록하지 않는다
- 그래프뷰에서 독립 섬으로 뜨는 것이 정상이다
- 이 폴더의 내용은 사용자 전용 — 마스터 세션 포함 모든 세션이 사용자의 명시 요청 없이는 읽지도 쓰지도 않는다

### 기술정리/ — 고립 구역 (기술블로그 정리본)

기술블로그를 읽고 **사용자가 먼저 정리한 글**을 채팅 세션이 다듬은 정리본을 쌓는 구역이다. 공개(깃·노션 동기화)지만 링크는 고립이다.

- 위키링크는 이 폴더 안에서만. 다른 구역과 양방향 모두 연결 금지, 공개 MOC·학습 지도에 등록하지 않는다
- 구조: `기술정리 MOC` → 주제 폴더(`Spring/` 등)의 정리 인덱스 → 글 노트. 폴더 안 링크는 자유
- **주제 폴더는 사용자가 지시할 때만 추가한다**
- **수정 최소화 원칙(중요)**: 정리본은 원 글의 내용을 흡수하려고 쓴 것이다. 부연설명·명백한 오류 교정·문단 흐름 정리만 하고, 재해석·재작성·요약 압축을 하지 않는다. 원본 보존 규칙이 강하게 적용된다
- frontmatter `출처: 내가 작성(Claude 교정)`, 본문 머리에 원문 제목·링크 명시
- `_인박스/`: 채팅 세션에서 완성한 .md를 넣는 드랍존. Cowork 스킬(tech-study-filing)이 형식화 후 주제 폴더로 배치한다
- **노트 간 연결은 잔차 트리 방식** (DELTAMEM, arXiv:2606.03083의 Residual Tree 차용 — 본문은 불변, 링크 구조만):
  - 주제 안에서 처음 다룬 글 = **Root**. 기존 글이 다룬 내용에 증분을 더하는 글 = **Δ(잔차)** — 머리말에 `> 기반: [[기존 글]]` 하나, 기반 글에 `> 잔차:`로 역방향 나열
  - 새 글이 기존 글에 **완전히 커버되면 배치 전 사용자에게 보고** (중복 기록 방지)
  - Δ끼리 가로 링크 금지 — 도달은 Root에서 잔차 체인 하강으로만
  - 같은 개념이 **두 글 이상에서 반복되면 개념 노트로 융합 승격**(공고화), 각 글에서 그리로 링크. 개념 노트끼리는 링크하지 않는다

### 세션 역할 분담

세션은 역할별로 나뉜다. **각 세션은 자기 관할 폴더만 쓰고, 다른 구역은 읽기 전용이다.**

| 세션 | 관할 (쓰기 가능) | 하는 일 |
| --- | --- | --- |
| **마스터** | `CLAUDE.md` · `_규칙/` · `_템플릿/` · `.gitignore` · 그래프 설정 · 최상위 폴더 구조 · Sync Engine·GitHub 인프라 | 규칙 제안 검토·확정, 인프라 운영. **노트 작업은 하지 않는다** |
| **KDT 학습** | 언어 폴더(Java·**Spring**·JavaScript·CSS·HTML·CS 이론·Python·RPA·AI) · `KDT_2026 학습 지도` · `Vault 홈` | VSCode 새 수업을 읽고 day 노트 작성·MOC 갱신. **Spring 수업(`2026B_Spring`)은 `Spring/` 폴더에 `Spring dayNN` 파일명으로** (Java 폴더 아님) |
| **보드게임카페 분석** | `프로젝트 코드 분석/보드게임카페예약사이트 프로젝트/` | 추가 분석 세션, 학습 관점 갱신 |
| **수집기** | `프로젝트 노트/` (`수집기` 접두사 노트) | 채용공고 수집기 프로젝트 기록 |
| **WMS** | `프로젝트 노트/` (`WMS` 접두사 노트) | 이커머스 WMS 3주 팀 프로젝트(Spring Boot+React)의 **프로젝트 기록만** — 기준: "이 프로젝트에서 무엇을 왜 선택했나" (MySQL을 고른 이유, 계층형을 고른 이유, 범위를 자른 기준 등). 파일명 `WMS NN - 제목`. **순수 리스트 변형**: 세부 파생 노트(`NN-1`) 없이 최상위 리스트만 — 깊은 내용은 노트 본문의 절로 담는다. 앞뒤 사슬·개념 노트(주제 색인)·MOC 주차 절은 동일 적용 |
| **WMS 정리** | `프로젝트 노트/WMS 정리내용/` | 사용자가 WMS를 하며 **기술 자체를 배운 것**(도커가 뭔지, 볼륨이 뭔지, 라운드 로빈이 뭔지 등)을 채팅 세션이 다듬은 .md로 배치하는 세션. 프로젝트의 선택·이유는 여기 아니라 WMS 기록 몫. **본문 내용 수정 금지**(기술정리와 같은 수정 최소화 원칙) — frontmatter·파일명·인덱스 등록만. 폴더 안에 정리 인덱스를 두고 `WMS 프로젝트 MOC`에서 인덱스로 한 줄 링크 (프로젝트 기록 리스트와 섞지 않는다) |
| **기술정리** | `기술정리/` | 인박스의 정리본을 형식화·배치 (내용 수정 최소화) |
| **코드훈련** | `코드정리/` · `코드실습/` | 코드정리 레퍼런스 기반 실습 미션 진행, 결과(원문+개선점)를 코드실습에 기록 |

- 새 프로젝트를 분석하면 그 프로젝트 폴더를 관할로 하는 세션을 새로 둔다
- 관할 밖 파일이 고쳐야 할 상태라면 직접 고치지 말고 해당 관할 세션(또는 마스터)에 요청한다. 규칙 변경은 "규칙 제안 채널"로

### 세션 시작 안내 (새 세션이 처음 읽을 것)

- **KDT 학습 세션**: 이 파일 전체 → `_규칙/옵시디언-노션-VSCode 운영 규칙.md`(특히 5·6·7항: 링크·문체·3단 구성) → `_템플릿/학습노트 템플릿.md`. 작업 흐름: `C:\Users\harry\Desktop\KDT_2026`에서 최근 수정 파일 확인(읽기 전용) → 해당 언어 폴더에 day 노트 작성 → MOC·학습 지도 색인에 등록 → git 커밋(한국어 메시지)
- **프로젝트 분석 세션**: 이 파일 전체 → `프로젝트 코드 분석/프로젝트 코드 분석 안내.md`(폴더·MOC·연결 규칙) → `_템플릿/프로젝트 분석 템플릿.md`. 자기 프로젝트 MOC가 진입점
- **공통**: 태그는 고정 어휘, KDT↔프로젝트 직접 링크 금지(학습 지도 매핑표 경유), 커밋은 작업 단위로

### 세션 권한 — 마스터 세션

이 Vault의 **규칙 관리 권한은 마스터 세션 하나에만** 있다.

- **마스터 세션**: 규칙·인프라 전담 Cowork 세션 (이 Vault의 구조·규칙을 만든 세션). 실작업(노트 작성·분석)은 각 관할 세션이 담당한다
- 마스터 세션만 수정 가능: `CLAUDE.md` · `_규칙/` 전체 · `_템플릿/` · 폴더 구조(최상위 폴더 생성/삭제/이름 변경) · `.gitignore` · 그래프 색상 그룹
- 규칙 수정은 **대화로 합의한 뒤 사용자의 "반영해줘" 신호에만** 실행한다 — 수시로 고치지 않는다
- 다른 세션은 위 파일들을 **읽기 전용**으로 취급한다. 규칙 변경이 필요하면 직접 고치지 말고 아래 "규칙 제안 채널"로 제안한다 — 마스터 세션이 검토 후 반영한다

### 프로젝트 노트/ — 연결 체계 (규칙제안 01·02 채택분)

**이 절은 `프로젝트 노트/` 폴더에만 적용된다.** 다른 구역(언어 폴더, 프로젝트 코드 분석, Fast-EQA)은 각자의 연결 규칙을 따르며, 이 절의 트리·앞뒤 사슬·개념 노트 규칙을 가져가지 않는다.

`프로젝트 노트/` 안에서 노트가 여러 개로 자라는 경우 아래 규칙을 따른다. 기본 골격은 **트리**, 가로 연결은 **개념 노트 관문 경유만** 허용한다. 여러 프로젝트(수집기·WMS 등)가 이 폴더를 공유하며, **파일명 접두사(`<프로젝트명> NN - 제목`)로 구분**하고 프로젝트마다 자기 MOC를 둔다. 프로젝트끼리는 링크하지 않는다.

- **번호가 계층이다.** `수집기 05` → `수집기 05-3` → `수집기 05-3-1`. 하이픈이 늘어날수록 상위에서 파생된 세부 노트다
- **번호는 배운 순서이며 바꾸지 않는다.** 읽는 순서는 MOC 표의 줄 순서로 관리한다 (파일명 변경은 Sync Engine의 VaultPath 기본키를 바꾼다)
- **링크는 한 칸씩만 오르내린다.** 자식은 머리말에 `> 상위:` 하나만, 부모는 `> 세부:`로 자식들을 나열한다. 한 칸 건너뛰기 금지
- **세부 노트끼리 직접 가로 링크는 어떤 경우에도 금지** (05-1 ↔ 05-2, 05-1 ↔ 06 전부). 세부 노트의 도달 경로는 부모 하강 하나로 고정한다 — "어디서 찾지?"의 답이 항상 같아야 한다
- **개념 노트** — 여러 가지에 걸친 개념(멱등성, 인코딩 등)은 폴더 안에 개념 노트를 만들고 각 지점에서 개념 노트로 링크한다. 가로 연결은 이 관문을 거칠 때만 존재한다. frontmatter는 `tags: [개념]`
  - 실제로 **두 곳 이상에서 같은 개념을 만난 뒤에만** 생성한다 ("관련 있어 보여서" 미리 만들지 않는다)
  - **개념 노트끼리는 링크하지 않는다** (관문들이 이어지면 다시 그물이 된다)
- **개념 태그** — 본문 인라인 태그(`#멱등성`)만 사용, frontmatter `tags`에는 넣지 않는다 (노션 multi_select 증가 방지). 개념 노트가 생기기 전 단계의 발견 채널
- **MOC 표에는 최상위 노트만** 올린다. 세부 노트는 상위 노트에서만 연결한다
- **`## 관련 노트`는 인덱스 앞뒤로 고정** (규칙제안 02, 2026-08-14 **채택**)
  - 기본 구성은 **번호가 하나 작은 최상위 노트 + 하나 큰 최상위 노트** 둘뿐이다. 뒤 노트가 아직 없으면 앞 노트만 둔다
  - 최상위 노트끼리만 잇는다. 세부 노트(`05-3` 등)는 `> 상위:` 하나만 두고 관련 노트 절을 두지 않는다
  - 번호가 배운 순서이므로 이 사슬은 곧 시간순이 된다. **판단이 필요 없는 링크**라 세션이 바뀌어도 일관되게 유지된다
  - 그 외의 연결이 필요해지면: ①여러 곳에 걸치는 개념이면 **개념 노트로 승격** → ②개념까지는 아닌데 직접 연결이 분명하면 **사용자에게 알리고 승인을 받은 뒤에만** 건다. 세션이 스스로 판단해 늘리지 않는다
  - **소급 적용한다** — 관련 노트는 과거 사실의 로그가 아니라 탐색 도구라, 노트마다 모양이 다르면 쓸모가 떨어진다. 기존 노트 전부 앞뒤 사슬로 정리했다 (2026-08-14 정정 — 최초 결정은 비소급이었으나 수집기 세션의 재논증으로 변경)
- **링크 기준** — 본문 중간 링크는 나중에 읽다가 정말 그리로 가고 싶을 때만 건다
- **경보 2개** (구조를 고칠 타이밍의 신호):
  - *그물화 경보* — 세부 노트에 부모 외의 링크를 걸고 싶어지면, 링크 대신 그 개념을 개념 노트로 승격할 때다
  - *깊이 경보* — 파생이 4단계(`05-3-2-1`)에 이르면 파생이 아니라 독립 주제가 된 것. 다음 번호(`05-4`)로 승격한다

> 요약: **트리 = 기본 키(주소, 유일한 도달 경로), 태그·개념 노트 = 보조 인덱스(위치를 바꾸지 않는 색인).** 그물은 인덱스 층에만 존재한다.

### 규칙 제안 채널 (고립 규칙의 유일한 링크 예외)

- 제안은 **제안 하나당 파일 하나**로 쓰고, 프로젝트마다 `<프로젝트명> - 규칙제안 기록` 장부에 목록·상태를 모은다
- 파일명: `<프로젝트명> - 규칙제안 NN - 제목`. 첫머리에 작성 세션 명시. 기각된 제안도 지우지 않는다 (이유와 함께 보존)
- **`[[CLAUDE]]` 링크는 프로젝트당 장부 하나만 걸 수 있다.** 개별 제안서는 장부에만 링크한다: `제안서 → 규칙제안 기록(장부) → CLAUDE`
- 역방향(CLAUDE → 장부) 링크는 걸지 않는다. 장부 외 노드가 CLAUDE에 링크하는 것도 금지
- 마스터 세션이 결정하면 제안서의 「결정 기록」과 장부 상태를 갱신한다 — **이 갱신과 링크 정리는 마스터 세션이 직접 수행할 수 있다** (마스터 세션의 관할은 CLAUDE.md와 그에 연결된 규칙 제안 문서까지 확장됨)
- 이 권한 구분은 지침 수준의 약속이다. 위반 여부는 git 이력으로 확인하고, 마스터 세션이 되돌릴 수 있다

### 다른 세션·프로젝트가 이 Vault를 쓸 때 (경계 규칙)

여러 Claude 세션(Cowork·Claude Code)이 이 Vault를 공유한다. 다른 프로젝트(예: 채용공고 크롤러) 세션은 다음을 지킨다.

- 노트는 자기 구역(`프로젝트 노트/` 등) 안에서만 작성·링크한다. 기존 학습노트·분석 노트·MOC·`_규칙/`은 수정하지 않는다
- **이 리포는 Public이다.** 공개되면 안 되는 내용(내부 URL, 키 등)은 Vault에 쓰지 않는다
- 채용공고 프로젝트 노트의 공개는 OK. 단, **비공식 소스에 대한 구체 크롤링 전략(대상·우회 방법 등)은 Vault 일지에 쓰지 않는다** — 그런 내용은 Private 코드 리포 쪽에만 남긴다
- `C:\Users\harry\obsidian-notion-sync`(Sync Engine 원본)의 `.env`·설정은 **수정 금지** — KDT Vault → Notion 미러링이 물려 있다. 재활용하려면 엔진을 복사해 별도 config로 돌린다
- `_규칙/sync-config.json`도 수정 금지 (기존 파이프라인 설정)
- git 커밋은 자유롭게 하되, 자기 구역 밖의 파일을 커밋에 섞지 않는다
